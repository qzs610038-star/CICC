# Codex Review Packet — myCobot V2.4 串口 Bug 修复 + 末段回零提速

> 生成: 2026-07-07
> 触发: AGENTS.md Codex Gate（myCobot 动作链路改动 + safe_return_home 签名变更 + 动作参数变更）
> 依据: trial_run_14_logs.md、return_ready_teach_replay_v2_plan.md §14.6/§14.7
> 改动文件: `mycobot_pc_tests/teach_replay_pick_return_ready.py`（唯一）
> 完整 diff: `mycobot_pc_tests/audit_logs/v2.4_codex_review_diff.patch`（240 行）

## 任务目标

1. 根治 trial_run_14 §1 的串口参数 Bug：`--save-preset` 等可选参数未配 `--port` 时被 `get_port()` 盲读为串口名。
2. 满足用户附加要求：加快最终回零响应速度（用户批 L1+L2+L3+L4 全套）。

## 当前结论

代码已落地 + `py_compile` 通过 + mock-serial 验证 `get_port()` Bug 已根治。属动作链路改动，未实机验证，等 Codex 复核 + run-15 实机验证。

## 修改或计划涉及的文件

- `mycobot_pc_tests/teach_replay_pick_return_ready.py`（唯一改动文件，84 增 23 删）
- **未改**: `teach_replay_pick.py`（方案 §0 禁止改基线）、`teach_and_pick.py`（无 argparse/无实际 bug/归档）—— git diff 确认为空。
- `mycobot_pc_tests/audit_logs/return_ready_teach_replay_v2_plan.md` 追加 §14.7（文档，非代码）。

## 关键模块与信号链路

- `get_port()`（行 ~233）：删 `if len(sys.argv) > 1: return sys.argv[1]` 两行。`main()` 已 `PORT = args.port if args.port else get_port()`，删盲读后 `get_port()` 只在 `--port` 未给时被调用，纯做 `comports()` 枚举+交互选择。
- `_verify_actual_pose_for_auto_return()`（行 ~773）：返回值由 `None` 改为"校验通过时的实际角度"（越界/走人工扶正/读不到仍返回 None）。
- `checked_return_transition()`（行 ~736）：透传上面返回的角度给 `auto_phase_v2` step10。
- `safe_return_home(mc, cached_angles=None)`（行 ~1076）：新增可选参数。
  - `cached_angles` 非 None：跳过自身 `get_filtered_angles` 重读，直接用它走 calc_home_diffs + 有限值校验 + arm_max_diff 门。
  - auto 分支：去掉纯诊断的 `diag_coords = get_filtered_coords()`（只打印不决策）。
  - manual 分支：保留 `diag_coords` 诊断（扶正场景有价值）。
  - `cached_angles=None`（默认）：行为与旧 `safe_return_home(mc)` 完全一致 → `prepare_phase` 的 `safe_return_home(mc)` 调用不变。
- `auto_phase_v2` step11（行 ~1308）：`safe_return_home(mc, cached_angles=home_ready_actual_angles)`，`home_ready_actual_angles` 来自 step10 `checked_return_transition` 的返回。

## 时钟、复位、AXI、framebuffer、双通道影响

无。本改动只在 PC 端 pymycobot 脚本层，不涉及 FPGA RTL / 时钟 / AXI / framebuffer / 双通道。

## 已运行验证

- 命令：`python -m py_compile mycobot_pc_tests/teach_replay_pick_return_ready.py`
- 结果：`PY_COMPILE_OK`（通过）
- 命令：mock-serial 探针（stub pymycobot + serial.tools.list_ports.comports=[]，`sys.argv=['x','--save-preset','my_new_test']` 调 `get_port()`）
- 结果：`get_port = ('EXIT', 1)`，不再返回 `"--save-preset"`，正确进入 comports 枚举/退出。`PASS_NO_BARE_READ`。
- 日志位置：无实机日志（未连真机）。run-15 实机日志待生成。
- 关键 warning：无。

## 机械臂 / 外设状态

- myCobot 是否涉及：**是**（L2/L3/L4 触及动作链路与动作参数；但本次未连真机，仅静态/mock 验证）。
- COM 口 / CP210x 状态：未探测（mock 验证不连真机）。
- 波特率：1000000（未改）。
- 是否执行动作：**否**（仅代码改动 + 静态/mock 验证，未触发任何运动命令）。
- 安全确认：L3 提速后固件不收敛会返回 0 → `safe_return_home` 返回 `"failed"` → 提示扶稳+release（安全失败，不撞机）。L2 `cached_angles` 仍走全部安全门判定。L4 微调失败仍走旧软通过门兜底。

## 未验证项和风险假设

- **L3 (`HOME_RETURN_SPEED` 20→25) 未实机验证**：run-12/run-14 只验证过 20。25 是否让固件 `is_in_position` 仍收敛未知；不收敛则 `res!=1 → "failed"`（安全失败）。run-15 须确认 step11 `res==1`；否则回退 25→20（不回 15）。
- **L2 `cached_angles` 路径未实机验证**：mock 验证不了真机读数时序。风险点：缓存的角度是否在 step10→11 之间仍代表臂当前姿态（臂静止在 home_ready，两读相隔~0.1s，理论一致）。若 Codex 认为有陈旧读数风险，可改为"缓存 + 单次 best-effort 复核"。
- **L4 (`SOFT_REFINE_TIMEOUT` 6→3s) 未实机验证**：run-14 step9 微调本就不收敛（plateau ~2.1°），3s 应足够判定；但若存在"4-5s 才收敛"的边缘工况，3s 会提前放弃走兜底。run-14 无此证据，按 best-effort 接受。
- 串口 Bug 修复对真机 comports 枚举路径未实机验证（mock 用空列表验证了不误读，真机多 CP210x 设备的交互选择路径沿旧逻辑未改）。
- 未恢复 `sync_send_coords()`、未放宽 `ARM_MAX_DIFF_SAFE`(45)/`HOME_READY_TARGET_ARM_MAX`(40)/`R_MAX`(280)/`SHORT_UP_SPEED`(16)/`SHORT_UP_TIMEOUT`(15)。

## 希望 Codex 判断的问题

1. `get_port()` 删盲读是否彻底、是否未误伤 `--port` 既有路径（`args.port` 优先于 `get_port()`）？
2. `safe_return_home(cached_angles=...)` 是否仅在 auto 分支省读数、是否仍走 calc_home_diffs + 有限值校验 + arm_max_diff 安全门（**不能跳过安全判定**）？
3. `cached_angles=None` 默认是否保证 `prepare_phase` 的 `safe_return_home(mc)` 旧调用行为完全不变？
4. L2 缓存角度是否存在"陈旧读数"风险？是否需要加单次 best-effort 复核？（臂在 home_ready 静止，两读相隔~0.1s，我判断无风险，请独立判断。）
5. `HOME_RETURN_SPEED=25` 是否仅在 arm_max_diff≤45 的末段生效、是否保留 `res!=1 → "failed" → 扶稳+release` 安全失败路径？
6. `SOFT_REFINE_TIMEOUT=3` 是否保留"微调失败走旧软通过门(3°/25mm)兜底"逻辑？
7. `teach_replay_pick.py` 与 `teach_and_pick.py` 是否确实未被修改？（git diff 已确认为空。）
8. 是否同意进入 run-15 实机验证？还是要求先回退某项（如 L3）再实机？
