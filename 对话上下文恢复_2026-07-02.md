# 对话上下文恢复：双摄视觉模块与 myCobot 布局

来源文件：

`D:/soft/wechat download/xwechat_files/wxid_c3t2g7whsefd22_fe0d/temp/RWTemp/2026-07/e5196a38e05a20b8e7ad629d84a69ab5/rollout-2026-07-02T15-03-44-019f21a4-0265-7f23-a33c-ba1becfc0d27.jsonl`

原会话时间：2026-07-02 15:03-17:09 左右。

原会话工作目录：`C:/Users/20306/Desktop/赛题资料`。

当前项目对应目录：`D:/第十届集创赛-雄芯院材料`。

## 1. 原始任务主线

用户希望推进第十届集创赛雄芯院方向的视觉模块方案。项目已有两路同参数摄像头、FPGA 视频链路、RISC-V CPU 和 myCobot 280 机械臂。用户要求通过连续提问的方式，逐步形成视觉模块推进流程。

整体路线逐步收敛为：

- FPGA 负责高速视频接入、ROI、颜色 mask、bbox、area、center 等统计特征。
- 板上 RISC-V CPU 负责颜色/尺寸/形状分类、阈值管理、标定、目标选择和机械臂控制。
- 不走纯 FPGA 视觉识别路线。
- 不走纯 FPGA 机械臂控制路线。
- 当前赛段优先跑通单物体、固定识别点、固定点抓取。
- 决赛扩展到多物体和第二路侧视相机形状辅助。

## 2. 项目文件中已核实的硬件/工程约束

原助手实际读取了项目目录和文档，得到以下约束：

- 主 Demo：`TJ375N529_SC431HAI2LCD_Demo_V3`
- 摄像头型号线索：项目名指向 `SC431HAI`
- 顶层已有两路 `soft_mipi_rx_top`
- 每路为 4-lane MIPI CSI
- 现有顶层参数分辨率按 `1920 x 1080`
- RAW10，`PACK_BIT = 40`
- 后端已有 `frame_buffer -> debayer_top_2to1 -> white_balance`
- 白平衡后为双像素 RGB888：`wb0_data_out[47:0]` / `wb1_data_out[47:0]`
- 现有显示链路当前主要接 `wb1_*`

原会话引用过的关键文件：

- `TJ375N529_SC431HAI2LCD_Demo_V3/src/top.v`
- `TJ375N529_SC431HAI2LCD_Demo_V3/src/mipi_csi/cam_i2c_ctrl/i2c/i2c_master_reg_rom.v`
- `分赛区决赛_FPGA_CPU协同工程框架.md`

注意：原环境里 `rg.exe` 被拒绝执行，原助手改用 PowerShell 文件遍历和 `Select-String` 检索。

## 3. 用户已给出的视觉目标与比赛条件

用户选择与补充：

- 第 1 阶段倾向先只用一路摄像头。
- 目标能力为颜色 + 尺寸 + 形状。
- 形状包括圆锥、圆柱、正方体。
- 机械臂只抓正方体，其他物体需要颜色/形状识别但不抓取。
- 采用 FPGA 提特征，CPU 分类和控制。
- 物体静止摆放等待识别。
- 当前赛段只出现一个物体，后续决赛可能多个物体同时出现。
- 物体摆放位置可自行决定，可固定到精确相对位置。
- 光照不可控，比赛当天才能获取。
- 最终需要区分圆柱和圆锥。
- 正方体、圆柱、圆锥都有 2.0cm / 2.5cm / 3.0cm 三档。
- 物体竖直放置。
- 不能放置背景板，具体背景色比赛当天才能获取。
- 先做固定点抓取，保留根据视觉中心点修正的进阶方案。
- 第二路摄像头安装由团队自行决定，但可能受线长影响。

## 4. 方案演化过程

### 4.1 初始建议：单俯视 MVP + 第二路形状增强

原助手判断：

- 只用一路俯视相机适合作为第一阶段 MVP。
- 但单俯视难以稳定区分圆柱和圆锥，因为两者俯视都可能接近圆。
- 最终稳定区分圆锥/圆柱/正方体，建议启用第二路侧视或斜视相机。
- 不建议一开始做双目测距，复杂度高、收益低。

建议验收顺序：

1. 颜色
2. 颜色 + 位置
3. 颜色 + 正方体/非正方体
4. 颜色 + 尺寸
5. 三类形状完整识别
6. 机械臂只抓正方体

### 4.2 双摄功能分工

在用户确认最终必须区分圆柱/圆锥后，方案收敛为：

- 相机 1：俯视，负责颜色、平面位置、外接框、面积、尺寸估计、机械臂抓取坐标。
- 相机 2：侧视或斜视，负责形状识别，尤其区分圆柱和圆锥。

原理由：

- 圆柱和圆锥俯视都可能是圆。
- 侧视下圆柱近似矩形，圆锥近似三角形。
- 正方体从俯视可看到方形顶面，侧视也近似矩形。

### 4.3 关于“斜 45 度侧视”的澄清

原助手说明，“斜 45 度侧视”指侧上方斜拍，不是双目，也不是两个摄像头互成 45 度。

坐标系定义：

```text
识别点中心为 O
桌面/赛台为 XY 平面
竖直向上为 Z
俯视相机在 O 正上方，镜头竖直向下
侧视相机可在 (D, 0, H)，镜头对准物体中心或中上部
光轴与桌面夹角约 30-45 度
```

后续用户质疑：直接正面拍摄不是也可以根据矩形/三角形区分圆柱/圆锥吗？

原助手修正：

- 第二路可以直接正面拍，不一定必须斜俯拍。
- 正面侧视能很好区分圆锥/非圆锥。
- 正面侧视对圆柱/正方体不够稳，因为两者正面轮廓都可能是矩形。
- 由于相机 1 已能区分方形顶面和圆形顶面，相机 2 可以更偏向侧视，略微下俯 5-15 度，用来区分圆柱/圆锥。

## 5. 机械臂和相机安装约束的更新

### 5.1 线长不足时的中间判断

用户曾补充：

- 当时估计相机最高约 20cm。
- 机械臂臂长最高可达 35cm。
- 担心垂直俯拍相机会干扰机械臂路径。

原助手当时判断：

- 不建议主相机固定在抓取点正上方做标准垂直俯拍。
- 建议改为偏置斜俯拍 + 侧视辅助。
- 相机 1 可放在识别点前方/侧前方 10-20cm，高度 12-20cm，光轴与桌面夹角约 45-60 度。
- 但斜俯拍会有透视变形，尺寸判断不能直接用原始 bbox，应做像素宽度标定和查表。

### 5.2 50cm 线到位后的最新判断

用户后来补充：

- 已买到长度 50cm 的线。
- 摄像头可以实现离地 30cm 以上。
- 机械臂比赛要求：从识别位置抓取后，横向移动大于 10cm 到分拣位置放下。
- 识别位置可自行决定。
- 分拣位置不确定是赛方提供还是自行决定。

原助手更新判断：

- 可以重新优先考虑高位俯视主相机。
- 但相机和支架不要放在机械臂必经路径正中间。

推荐：

```text
相机 1：高位俯视 / 轻微偏置俯视
  高度：30-40cm
  角度：尽量接近垂直向下
  作用：颜色、尺寸、顶面形状、目标中心、抓取校验

相机 2：侧视或轻微俯视侧视
  高度：3-10cm 或物体中上部高度
  角度：基本水平，略向下 5-15 度
  作用：区分圆柱 / 圆锥
```

## 6. myCobot 280 文档归档与关键参数

原会话中用户提供文件：

`C:/Users/20306/xwechat_files/wxid_zct55i0rjy2s22_5da8/temp/RWTemp/2026-07/e5196a38e05a20b8e7ad629d84a69ab5/mycobot 280 for arduino技术参数与官方网址(3).txt`

原助手已复制到原项目：

`C:/Users/20306/Desktop/赛题资料/docs/mycobot_280_for_arduino_技术参数与官方网址.txt`

读取时文档乱码，但关键参数可识别：

- 机械臂：myCobot 280 for Arduino
- 自由度：6 DOF
- 最大工作半径：280mm
- 最大负载：250g
- 机械臂重复定位精度：±0.5mm
- 夹爪夹持范围：20-45mm
- 夹爪重复精度：1mm
- 夹爪重量：约 100g
- 夹爪最大夹持力：150g
- 控制接口：串口控制

注意：当前新项目路径下是否已有该文档，需要重新检查并复制到 `D:/第十届集创赛-雄芯院材料/docs/` 或合适资料目录，不能默认原路径仍存在。

## 7. 机械臂与识别/分拣区推荐范围

基于 myCobot 280 工作半径 280mm，原助手建议：

- 机械臂底座到识别点距离：140-180mm。
- 初始可按 160mm 布置。
- 识别点到分拣点水平距离：120-160mm，可更大，但要留在舒适工作区。
- 分拣点到底座距离：控制在 140-220mm。
- 避免识别点或分拣点放到 >230mm 的边缘区域，否则末端姿态更难。
- 抓取精度目标：
  - 最低可用：中心摆放/抓取误差 ≤ ±8mm
  - 推荐目标：≤ ±5mm
  - 稳定比赛目标：≤ ±3mm

## 8. 最后一轮用户已给出的点位答案

原助手最后要求确定坐标系和点位，建议以机械臂底座中心为原点：

```text
O = 机械臂底座中心
X 轴 = 从机械臂底座指向识别区方向
Y 轴 = 水平横向，右手方向
Z 轴 = 竖直向上
单位 = mm
```

用户最后回复：

1. 可以确保机械臂底座、识别点、摄像头相对位置始终不变。
2. 前期调试由于支架缺失，摄像头位置可能有小范围误差，约 ±1cm。
3. 识别点放在机械臂左前方。
4. 识别点到机械臂底座中心可先按 160mm 布置。
5. 分拣点在识别点右侧 120-160mm，可以更大。
6. 相机 1 可以安装在识别点正上方附近，高度 300-400mm，允许轻微偏置。
7. 夹爪从上往下抓取。
8. 机械臂为 6 自由度，但赛方要求至多使用 5 个自由度；团队计划冻结 1-2 个次要自由度。
9. 用户要求只考虑冻结自由度对视觉算法的影响，机械臂控制影响由队友负责。

原会话在这里结束，没有生成下一轮收敛结论。

## 9. 从最后一轮应继续给出的结论

基于用户最后答案，下一步应先给出坐标表和视觉影响判断。

建议采用以下暂定坐标：

```text
全局原点 O：机械臂底座中心
单位：mm

识别点 P_pick:
  方向：左前方
  建议坐标：X = 140, Y = +75, Z = 0
  水平距离：sqrt(140^2 + 75^2) ≈ 159mm

分拣点 P_bin:
  在识别点右侧 120-160mm
  如果 Y 正方向为左侧，则“右侧”可理解为 Y 减小
  建议坐标：X = 140, Y = -55 到 -85, Z = 0
  与识别点距离：130-160mm
  到底座距离约 150-164mm，仍在舒适区
```

注意：上面的 `Y = +75` 只是为了表达“左前方”，实际正负方向需要团队在物理桌面上贴坐标纸确认。

相机 1 建议：

```text
主相机 C1:
  位置：识别点正上方或轻微偏置
  高度：Z = 300-400mm
  水平偏置：30-60mm 内
  角度：尽量垂直向下
  支架：不要穿过机械臂从上往下抓取的路径
```

相机 2 建议：

```text
侧视相机 C2:
  放在不影响机械臂进场的一侧
  高度：Z = 30-100mm，优先对准物体中上部
  角度：基本水平，略微下俯 5-15 度
  作用：圆柱/圆锥区分
```

冻结 1-2 个自由度对视觉算法的影响：

- 视觉不要假设机械臂能从任意角度修正抓取。
- 第一阶段应把物体识别点和抓取姿态固定化，视觉只输出目标是否可抓和中心偏差。
- 若冻结腕部旋转，正方体的朝向不应成为抓取必要条件；识别区应尽量让正方体边缘方向固定，或夹爪宽度留足容差。
- CPU 侧可先不做复杂姿态估计，只做 `center_x/center_y` 偏差监控和 `grasp_ok` 判定。
- 后续视觉闭环修正应限制在小范围 XY 偏移，例如 ±5mm 到 ±10mm，不要试图让视觉补偿大姿态误差。

## 10. 下一轮应继续问的问题：FPGA/CPU 寄存器字段

原助手计划在坐标系后继续问 FPGA 寄存器字段。下一轮可直接进入这些问题：

1. 第一版是否只处理一路主相机 `wb1_*`，第二路先显示/保留？
2. ROI 是固定写死在 RTL 参数里，还是由 CPU 通过寄存器配置？
3. 颜色分类先支持哪些颜色？是否确认为红、蓝、黄、白、黑？
4. FPGA 输出每帧一个最大连通目标，还是每种颜色各输出一个候选目标？
5. 当前赛段单物体时，是否允许只输出一个最大 mask 的 bbox/area/center？
6. CPU 是否需要读到原始颜色统计值，例如 `sum_r/sum_g/sum_b`，用于比赛当天标定阈值？
7. 是否需要 OSD 显示 bbox、center、颜色类别和 `grasp_ok`，用于现场调试？

建议第一版寄存器字段：

```text
VISION_STATUS
  bit0 frame_valid
  bit1 target_valid
  bit2 roi_overflow / stats_overflow
  bit3 threshold_ready

FRAME_ID

ROI_CFG
  roi_x0, roi_y0, roi_x1, roi_y1

TARGET_BBOX
  xmin, ymin, xmax, ymax

TARGET_CENTER
  center_x, center_y

TARGET_AREA
  mask_area

TARGET_COLOR_SUM
  sum_r, sum_g, sum_b 或均值 mean_r, mean_g, mean_b

TARGET_CLASS_RESULT
  color_id
  size_id
  top_shape_id
  grasp_ok

DEBUG_THRESHOLDS
  color threshold / ratio threshold
```

第一阶段可以先做统计寄存器，不急着在 FPGA 内输出最终 `color_id/size_id/shape_id`；这些分类更适合 CPU。

## 11. 当前接续建议

如果继续推进，建议下一步不是立刻改 RTL，而是先生成一份 `视觉模块实施路线_坐标与寄存器_v1.md`，把以下内容定版：

- 坐标系和识别点/分拣点坐标。
- 相机 1 / 相机 2 安装位置与禁入区。
- 当前赛段只启用相机 1 的验收目标。
- FPGA 统计特征列表。
- CPU 分类和标定职责。
- OSD 调试显示项。
- 现场标定流程。
- 第一版不做/后续再做的事项。
