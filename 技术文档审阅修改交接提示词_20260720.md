# 技术文档审阅结论与修改交接提示词

## 一、审阅结论

本文件用于交给新的 Codex 窗口继续修改 `D:\CICC w\技术文档.md`。当前只完成审阅与提示词编制，没有改动技术文档正文。

审阅依据包括：

- `D:\CICC w\CICC技术文档与PPT收集整理\01_第九届官方优秀作品专刊` 中 11 篇作品的 Markdown 抽取文本与写作共性总结；
- 与本项目最接近的第 04、06、10、11 篇原始 PDF，重点抽查首页、架构、系统实现、测试和结论页面；
- `D:\CICC w\AGENTS.md`、`CURRENT_STATE.md`、`SESSION_HANDOFF.md` 和官方 0710 比赛细则；
- `技术文档.md` 当前全文；
- 代码图谱中与快照、配置提交、轮次控制和动作单发有关的实现定位。图谱只用于定位，事实判断仍以真实源码、日志和状态文件为准。

### 1. 优秀作品中真正值得借鉴的写法

1. 摘要直接给出问题、方法、平台和量化结果，不用行业背景套话。
2. 总体架构之后立即解释模块分工、数据流和选择该架构的原因，不只放一张框图。
3. 每个关键模块遵循“问题—方法—实现—验证”的顺序，结论紧跟证据。
4. 结果章节说明测试对象、环境、样本或重复次数、判据、结果和误差来源；架构图和示意图不冒充实测结果。
5. 系统类作品会给实物环境、功能过程和对照表。性能数字都有测量条件，较弱指标和局限也会如实说明。
6. 结论重述最重要的工作和指标，同时写明具体限制；不使用“全面提升”“具有重要意义”等无数据判断。

### 2. 当前技术文档最需要修正的问题

按优先级排序如下：

1. **官方规则引用错误。** 正文和参考文献写了“0715 修正版”，但仓库权威入口是 0710 比赛细则。没有找到并核对 0715 原件前，不得继续引用 0715。
2. **评分比例存在原文冲突。** 官方 0710 细则文字说明采用识别 25%、判断 25%、执行 50%，但评分表中任务三每轮为 `1/1/1`，即三项各占三分之一。正文不能把 25%/25%/50%无条件写成四项任务统一比例。应保留各任务绝对分值，并注明任务三比例口径需现场确认。系统设计可依据不冲突的共同规则：识别错误阻断后续得分，目标物必须有明确且唯一的机械臂响应。
3. **摘要和结论扩大了完成范围。** “CPU完成分类并生成OSD和机械臂请求”“软硬件模块均通过功能检查”“设计并实现整套系统”等表述容易被理解为板级闭环已打通。当前证据只支持模块实现、Host/fake transport 测试、候选工程冷构建和 PC 端独立动作验证，不支持真实 APB、CPU→OSD、板上逐轮事务或 myCobot 板控闭环。
4. **把代码存在写成了顶层已接通。** 代码中确有 `frame_id/ACK`、配置提交、轮次控制和 `arm_request_sent`，但当前 CPU 主循环明确保留“正式事件/APB来源尚未接入、动作请求应不可达”的边界。修改时必须核对顶层和真实 transport，不能凭函数存在宣称整链完成。
5. **正式主线与隔离候选的证据边界不清。** Efinity 冷构建来自 `competition_project_single_camera` 固定输入批次，它不能自动证明 `final_project` 正式主线或整机板级闭环通过。
6. **板级状态写得过满。** `CURRENT_STATE.md` 明确列出 `USER2`、CPU实际取指、UART0横幅/回显、APB实读、正式MMIO ABI、CPU→OSD、正式目标输入、逐轮板级事务、UART2/J52和myCobot板控闭环均未验证。文档可以写设计与离线实现，不能写成板上已经运行。
7. **任务三、四缺少目标集合真值表。** 任务三参考物为 2 cm 时应抓 3 cm，参考物为 3 cm 时应抓 2 cm；五个物体中通常有两个目标。任务四目标尺寸为 2 cm 或 3 cm，与其差值不超过 0.5 cm 的目标包括相同尺寸物体和唯一的 2.5 cm 物体；五个物体中通常有三个目标。
8. **分类算法说明不足。** 颜色规则缺阈值、判决次序、未知条件和光照影响；形状仅凭填充率区分正方体、圆柱体和锥体，容易受视角、姿态和遮挡影响；尺寸仅凭包围盒高度，缺少相机固定条件、三点标定、阈值边界和误差处理。没有实测样本时不得补造准确率。
9. **接口完成态用语不严谨。** `soc.h`、APB基址、字段偏移和 staging/commit/active 不能仅凭候选契约写成正式接口已定版。需区分逻辑协议、CPU侧实现、RTL侧实现、顶层连线和板级实读五个层次。
10. **机械臂证据被放在过大的章节标题下。** 5/5、177.72°和2.02°来自 PC 端五点路径验证，只能支撑点位顺序、旋转方向和调试策略，不能证明板上 CPU、UART2/J52、识别触发、最大臂展和端到端分拣通过。
11. **验证章节缺少“不能证明什么”。** 182/182、3/3、20个确定性事务、WNS/WHS、bitstream/ELF hash均应附对象、环境和证据路径；不能只列 PASS。Efinity 的 route 资源、器件资源和物理 IO 还需准确区分。
12. **论文结构不完整。** 当前没有真正的绪论、相关方案不足和三条本文贡献，全文更像工程说明书。参考文献中内部计划、Review Packet和公开文献混排，也不符合正式技术报告习惯。
13. **正文有内部治理口吻。** `Gate`、`Review Packet`、`bundle`、固定 SHA 等适合放在附录“版本与证据索引”，不宜反复出现在摘要和技术原理段落。
14. **标题可能被质询。** “具身智能”和“芯机协同”容易显得概念拔高。若作品名尚未锁定，建议采用更可验证的标题；若已锁定，必须在绪论中给出与本系统相符的具体定义。

### 3. Handoff 健康检查说明

2026-07-20 运行 `tools\agent_handoff_health_check.ps1` 时，仓库路径、Git、关键文件和可读性检查均通过；唯一失败项是系统找不到 `python` 命令，导致 myCobot 只读检查未运行。该结果只表示工具环境缺失，不能推断机械臂通过或失败。新窗口应使用 Codex bundled Python 重跑相应只读检查，或把此项明确记录为未执行。

---

## 二、交给新窗口的完整提示词

将下面代码块整体发送给新窗口。新窗口应直接修改文件，不应只返回一份建议清单。

```text
请直接修改 `D:\CICC w\技术文档.md`，把它整理为可提交给第十届全国大学生集成电路创新创业大赛分赛区决赛评委阅读的正式技术报告。不要只给修改建议；请完成实际改稿、事实复核和最终检查。不要修改源码、工程XML、SDC、IP、官方细则、CURRENT_STATE.md或机械臂脚本，也不要覆盖工作区中与本任务无关的现有修改。

一、开始前必须执行的检查

1. 完整阅读并遵守：
   - `D:\CICC w\AGENTS.md`
   - `D:\CICC w\CURRENT_STATE.md`
   - `D:\CICC w\SESSION_HANDOFF.md`
   - `D:\CICC w\final_project\docs\competition_manual\第十届集创赛分赛区决赛雄芯院企业命题比赛细则_0710.md`
   - `D:\CICC w\.agents\skills\fpga_vision\SKILL.md`
   - `D:\CICC w\.agents\skills\cpu_mycobot\SKILL.md`
   - `D:\CICC w\技术文档审阅修改交接提示词_20260720.md`
2. 运行 `tools\agent_handoff_health_check.ps1` 和 `git status --short --branch`。若健康检查只因系统找不到 `python` 命令而失败，使用 Codex workspace dependencies 提供的 bundled Python重跑有关的只读检查；仍无法运行时，记录为工具环境缺失。不得据此推断myCobot通过或失败。
3. 阅读优秀作品专刊：
   - 完整阅读 `优秀技术文档写作共性总结.md`；
   - 重点阅读第04、06、10、11篇原始PDF或完整抽取文本；
   - 其余作品至少阅读摘要、总体方案、验证和结论。
4. 代码发现优先使用codebase-memory-mcp的 `search_graph`、`trace_path`、`get_code_snippet` 和 `search_code`。图谱只用于定位，所有完成度结论必须回到真实源码、顶层连线、工程XML、日志、Review Packet和 `CURRENT_STATE.md`。
5. 先检查 `技术文档.md`、`技术文档.docx` 和 `技术文档_assets` 的关系。本次以Markdown为唯一内容真源；未经确认不要同步改DOCX，以免两个版本分叉。

二、总体改写原则

1. 写成正式中文技术报告，不写成仓库状态汇报、源码README或项目宣传稿。
2. 行文客观、克制、具体。优先使用“本文设计”“模块接收”“控制器在……条件下转移”等有明确主语和条件的句子。
3. 严禁编造识别准确率、抓取成功率、耗时、资源、时序、样本数、板级现象、机械臂动作或实拍结果。
4. 区分以下证据层级，不得互相替代：
   - 设计方案或接口契约；
   - 源码/RTL模块存在；
   - 模块级仿真或Host/fake transport测试；
   - Efinity综合、布局布线、STA、CDC和bitstream生成；
   - UART0 ELF静态审计；
   - PC端机械臂点位验证；
   - 真实板级接口验证；
   - 端到端比赛闭环验证。
5. “保证”只用于可由结构、断言或测试证明的性质；其余改成“用于避免”“在……条件下保持”“降低……风险”。
6. 不照抄优秀作品。借鉴其“问题—方法—实现—测试—结论”的逻辑，技术内容只能来自本项目。

三、必须先修正的事实问题

1. 把正文和参考文献中的“0715修正版”改为仓库权威的“0710比赛细则”。如果发现真正的0715原件，必须先核对其来源和正文，再决定是否引用。
2. 修正评分比例表述。官方0710正文写识别25%、判断25%、执行50%，但任务三评分表为每轮 `1/1/1`。不得写成四个任务都严格服从同一比例。采用以下处理：
   - 表格保留任务一至四的绝对分值；
   - 正文说明识别错误阻断后续得分、目标物必须有唯一响应等无冲突规则；
   - 单列注释说明任务三比例文字与评分表不一致，现场口径需赛前确认；
   - 不自行替组委会解释或改写规则。
3. 摘要中删除或缩小“软硬件模块均通过功能检查”等笼统结论。准确写出已有结果及层级：候选工程固定输入批次完成Efinity冷构建；CPU Host/fake transport测试和Python离线回归通过对应测试；这些结果不等于板级闭环。
4. 明确 `final_project` 是正式协作主线，`competition_project_single_camera` 是隔离候选。候选工程的bitstream、STA、CDC和warning只支撑对应固定输入批次，不能外推到正式主线整机。
5. 在摘要、架构、接口、机械臂和结论中明确：当前未验证 USER2、CPU实际取指、UART0 115200横幅/回显、APB实读、正式MMIO ABI、CPU→OSD、正式目标输入、逐轮板级事务、UART2/J52和myCobot板控闭环。
6. 代码中有快照、提交、轮次控制和动作单发实现，但当前CPU主循环的正式事件/APB来源尚未接入，动作请求路径按设计应不可达。除非你从当前真实源码和顶层连线证明状态已经变化，否则只能写“模块级实现/离线验证”，不能写“整链已接通”。
7. `ARM_NOT_READY`是不动作的安全降级，不是比赛任务成功。写清它避免盲动，但目标物未执行会丢失执行环节得分。
8. OSD图只能标为“版式示意”，不能称为板级显示实拍或测试结果。
9. UART0使用115200 baud，myCobot链路使用1000000 baud。两条链路、端口和验证Gate始终分开，不能相互外推。
10. PC端5/5动作结果必须改放到“PC端动作路径与点位验证”小节。177.72°是由点位坐标推导的方位变化，2.02°是特定阶段的关节软到位误差；二者不能替代官方180°±10°、最大臂展和完整分拣验收。

四、建议采用的章节结构

可以在保留有效素材的基础上重构，不必机械套用标题：

摘要：用约300至450字说明具体问题、系统分工、三项核心方法、已有量化结果和证据边界。不要为了“像优秀作品”硬凑数字。

第1章 绪论：比赛需求与工程约束；纯RTL集中决策、完整帧搬运给CPU等备选方案的具体不足；本文工作与贡献。贡献限定为三条，每条写清做了什么、解决什么、由什么证据支撑。没有文献或数据时不要写“首次”“领先”“显著提升”。

第2章 系统需求与总体架构：赛题要求与实现映射；硬件/软件平台；FPGA、片上RISC-V、OSD和机械臂的职责边界；数据流、控制流和单轮工作流程。总体架构图应标出已实现模块与尚未形成板级证据的接口，避免一条连续箭头暗示全链已打通。

第3章 FPGA视频前端与帧级特征：2PPC数据格式、ROI、像素掩码、累计、bbox、帧快照、无回压代价、ACK与丢帧行为、CDC边界及模块仿真。减少源码长段，优先用公式、状态条件、数据流图和必要伪代码。

第4章 CPU分类、任务判断与轮次事务：颜色、形状、尺寸分类；任务一至四真值条件；START/END与事件序号；动作单发；结果语义和原因码；低置信度、超时、重复事件和错误输入的处理。

第5章 OSD与机械臂执行：CPU生成语义、FPGA负责渲染；myCobot帧和响应匹配；动作状态机；点位、到位判据和安全条件；PC仅用于开发期点位验证，不进入正式闭环。

第6章 实现与分层验证：RTL仿真、CPU Host/fake transport、Python离线回归、Efinity冷构建、UART0 ELF静态审计、PC动作路径、板级端到端验收。每一层均列测试对象、工具/版本、输入、判据、结果、证据路径和不能证明的事项。

第7章 结论与限制：只总结证据支持的工作和指标；明确当前最重要的限制，如系统级准确率、端到端耗时和板控闭环尚无同批次实测。控制在150至250字，不逐章复述全文。

附录：版本与证据索引、warning分类、逻辑寄存器分组、复现命令和团队分工。SHA、hash、内部Review Packet和Gate信息尽量移到附录，不与公开参考文献混排。

五、必须补强的技术内容

1. 增加“赛题要求—实现模块—输出—验证层级”映射表。
2. 增加平台表，列器件、版本、接口和用途；只填真实可查信息。
3. 增加分类特征表：输入特征、判决方法、未知条件、参数来源、主要误差来源。
4. 颜色识别要说明红/蓝/黄面积的比较逻辑、白/黑亮度逻辑、判决次序、阈值来源和低置信度条件。讨论补光、白平衡、阴影和高光影响，不编造准确率。
5. 形状识别要准确说明填充率能证明什么、在何种固定视角和姿态下成立。核对源码是否还有宽高比或其他特征；没有就不要凭空补算法。对投影相似、遮挡和低置信度输出未知作限制说明。
6. 尺寸识别要区分尺寸分类和尺寸关系判断。说明相机/台面固定条件、2/2.5/3 cm三点标定、分类边界、误差容限和越界处理；没有标定数据就给验收方法，不给虚假结果。
7. 增加四任务真值表：
   - 任务三参考2 cm时，3 cm为目标；参考3 cm时，2 cm为目标；
   - 任务四目标2 cm时，2 cm和2.5 cm为目标；目标3 cm时，2.5 cm和3 cm为目标。
8. 解释一个任务内存在多个目标时，轮次锁只锁当前物体事务，不能跨五轮阻止后续目标响应。
9. 分别解释 `frame_id/ACK`、staging/commit/active、事件序号和 `arm_request_sent`解决的问题。核对ACK跨域、旧ACK、新快照、序号回绕以及顶层接线。模块级实现不得写成板级CDC已经验证。
10. myCobot协议中的帧长、命令ID、大小端、角度单位和响应长度应核对官方来源版本。区分字节已发送、响应匹配、关节到位、夹爪闭合和物体夹牢五种状态。
11. 覆盖轻取轻放、提前释放/跌落、180°±10°、最大臂展、动作唯一性和故障停机。没有板级证据时只写设计约束与验收方法。

六、验证与图表要求

1. 建立分层验证矩阵，至少包括：
   - RTL模块仿真；
   - CPU Host/fake transport测试；
   - Python离线bundle回归；
   - Efinity Map/Interface/PNR/STA/CDC/bitstream；
   - UART0 ELF静态审计；
   - PC端点位动作；
   - 板级端到端闭环。
2. 逐项核实 182/182、3/3、20个确定性事务、WNS 1.321 ns、WHS 0.026 ns、bitstream/ELF hash、5/5、177.72°和2.02°的原始证据。找不到证据的数字从结论中删除或标为待核实，不得沿用二手摘要。
3. Efinity结果应说明固定输入批次、工具版本、约束背景、setup/hold结果和warning分类。WNS/WHS缺少时钟约束背景时，不要解读为系统性能。Inputs/Outputs/XLRs若是route资源，必须使用准确名称，不能写成物理IO占用。
4. 增加系统级验收指标表，但不得填造结果。可列测量方法和判据：五色/三形/三尺寸混淆矩阵、每类样本数、识别准确率、判断准确率、抓取成功率、跌落次数、单轮耗时、四任务总耗时、放置角度误差和最大臂展位置偏差。
5. 所有结果图必须来自真实波形、日志、统计或照片。总体架构图、状态机图和OSD示意图不能放在实验结果中冒充实测。
6. 每张图先在正文引用，再出现图和图题；图后说明能读出什么、测试条件是什么、不能证明什么。检查图片路径存在、图表编号连续、无未引用资产。
7. 公开参考文献与内部工程证据分开。公开文献补齐作者/机构、题名、文献类型、版本或日期；内部计划、Review Packet、寄存器契约和hash放“工程证据索引”。

七、去AI味的具体语言规则

1. 删除或改写无证据套话：赋能、打造、全面提升、具有重要意义、提供有价值的参考、显著、领先、首创、完美解决、三项核心设计贯穿全文。
2. 不使用固定的模型式排比。不要让连续三段都采用“首先—其次—最后”或“通过……实现……从而保证……”结构。
3. 一句话只承担一个主要判断。长句拆分后保留明确因果，不用多个抽象名词堆成口号。
4. 技术结论必须包含对象、条件、动作和结果。例如：“快照valid保持期间，模块拒绝覆盖新帧统计并递增 `dropped_frames`。该策略保持已发布字段的一致性，但会丢弃中间帧。”
5. 主动说明代价和适用条件：快照保持会丢帧；规则分类依赖现场标定；Host测试不覆盖真实MMIO；PC动作不等于板控。
6. 减少路径、函数名、反引号和代码块。仅当名称对理解机制不可替代时保留，其余移到附录。
7. 术语首次出现给出中文全称和英文缩写，后文统一。统一 `2PPC`、`32 bit`、数值与单位空格、图表编号和中英文标点。
8. 若保留“具身智能”“芯机协同”“原子协议”“事务”“实时”“低带宽”“无回压”“最大臂展”等词，必须给出本文语境下的明确定义或可检查条件。
9. 不用“显而易见”“不难看出”“我们认为”“效果良好”“稳定运行”等模糊判断。
10. 摘要和结论不要逐句重复；结论必须含一个具体限制和后续验收方向。

八、最终交付要求

1. 直接完成 `D:\CICC w\技术文档.md` 的修改，不另建一份平行正文。
2. 修改后再次读取 `CURRENT_STATE.md`，逐句检查摘要、结果和结论是否越过当前证据边界。
3. 输出一份“数字—含义—证据路径—证据层级”清单。没有原始证据的结果数字不得留作完成结论。
4. 检查官方任务、轮数、绝对分值、目标集合、10分钟、180°±10°、最大臂展和跌落定义与0710原文一致；任务三比例冲突必须保留现场确认说明。
5. 检查所有完成、通过、实现、稳定、实时、保证、闭环等词，每处均应有对象、条件和证据；否则缩小表述。
6. 检查final_project/隔离候选、离线/板级、UART0/myCobot、PC调试/正式闭环的边界是否清楚。
7. 检查图表编号、图片路径、正文引用、公式变量、单位、缩写和参考文献格式。
8. 不修改或删除用户的既有无关文件，不清理工作区dirty内容。
9. 最终回复给出：关键改动清单、已核实数字及证据路径、仍缺原始证据清单、仍不能声称通过的板级项目。
```
