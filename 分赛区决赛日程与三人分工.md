# 分赛区决赛日程与三人分工

> 项目：第十届集创赛雄芯院方向  
> 阶段：分赛区决赛  
> 目标日期：2026-07-20 完成可演示、可评分、可解释的保底系统  
> 版本：v0.1，先按默认三人小队拆分，后续可按实际能力重排  
> 关联文档：`分赛区决赛实施开发路线.md`

## 1. 分工原则

三人默认按职责拆成：

- A：视觉与 FPGA 前端负责人。
- B：板上 CPU、机械臂与控制闭环负责人。
- C：集成、测试、文档与答辩负责人。

正式技术路线按方案 B 执行：

- FPGA 负责 MIPI 摄像头接入、RAW/ROI/统计特征、HDMI 显示基础。
- 板上 CPU 负责颜色/形状/尺寸分类、现场任务参数、`cube_only_guard`、机械臂动作状态机和 myCobot 串口控制。
- PC + `pymycobot` 只用于前期机械臂调试，不进入最终比赛闭环。

## 2. 三人职责总表

| 角色 | 主责 | 必交产物 | 协作边界 |
|---|---|---|---|
| A：视觉与 FPGA 前端 | 摄像头接入、MIPI/RAW、ROI、特征统计、HDMI 输出、基础 OSD | 稳定视频链路、ROI/统计特征输出、HDMI 显示状态、资源/时序日志 | 向 B 提供 CPU 可读的特征数据；向 C 提供演示画面和构建证据 |
| B：CPU 与机械臂闭环 | CPU 分类逻辑、任务判定、参数切换、myCobot 串口协议、动作序列、安全互锁 | `vision_classifier`、`task_matcher`、`arm_controller`、点位表、复位/急停流程 | 从 A 读取特征；向 C 提供动作流程、串口日志和异常处理说明 |
| C：集成测试与文档 | 每日集成、评分项测试、日志归档、技术文档、PPT、演示视频 | 测试表、问题清单、演示视频、资源统计摘要、答辩材料 | 推动 A/B 每天交可验收产物；记录失败原因和修复闭环 |

## 3. 阶段日程总表

| 阶段 | 日期 | 阶段目标 | 主责 | 阶段验收 |
|---|---|---|---|---|
| 1 | 7 月 1 日 - 7 月 3 日 | 工程骨架与硬件链路确认 | A 主，B/C 协作 | 摄像头到 HDMI 可显示；机械臂可安全通信；干净工程结构建立 |
| 2 | 7 月 4 日 - 7 月 7 日 | 识别保底闭环 | A+B 主，C 测 | FPGA 输出特征，CPU 输出颜色/形状/尺寸识别结果 |
| 3 | 7 月 8 日 - 7 月 11 日 | 机械臂动作闭环 | B 主，A/C 协作 | `match=yes` 抓取，`match=no` 跳过，连续 5 次可运行 |
| 4 | 7 月 12 日 - 7 月 15 日 | 分赛区评分项专项打磨 | C 主持，A/B 修 | 三类评分项多轮 5 次测试，目标达到 80% 以上 |
| 5 | 7 月 16 日 - 7 月 18 日 | 稳定性与文档证据 | C 主，A/B 供证 | 固化 bitstream/参数/点位；完成演示视频和资源证据 |
| 6 | 7 月 19 日 - 7 月 20 日 | 冻结与应急预案 | C 主持，全员 | 冻结主版本；完成现场操作手册和备用方案 |

## 4. 每日执行表

| 日期 | A：视觉/FPGA | B：CPU/机械臂 | C：集成/文档 | 当日验收物 |
|---|---|---|---|---|
| 7 月 1 日 | 梳理现有 Demo 中 MIPI、RAW、HDMI、时钟、约束模块 | 确认 myCobot 连接方式、串口参数、PC 调试环境 | 建立测试记录模板、问题清单、文档目录 | Demo 复用清单、机械臂通信待测清单、测试表模板 |
| 7 月 2 日 | 跑通摄像头到 HDMI 显示，记录分辨率/lane/RAW 位宽 | 用 PC + `pymycobot` 做只读状态或极小幅安全动作 | 录制链路跑通视频，记录线缆/供电/端口 | HDMI 采集画面证据、机械臂安全通信证据 |
| 7 月 3 日 | 建立干净工程骨架，迁入必要视频链路模块 | 整理机械臂点位需求和动作序列草案 | 汇总阶段 1 验收报告 | 干净工程目录、第一版动作流程图 |
| 7 月 4 日 | 实现固定 ROI 和基础像素统计输出 | 定义 CPU 侧特征输入结构体和分类参数表 | 准备 5 色/3 形状/3 尺寸样本测试表 | ROI 坐标、特征字段定义 |
| 7 月 5 日 | 完成颜色均值/直方图/亮度统计 | 实现颜色分类初版，加入亮度归一化和颜色比值 | 做不同环境光下颜色测试记录 | 五色分类初版结果 |
| 7 月 6 日 | 提供面积、边界框、长宽比等形状/尺寸特征 | 实现形状分类和尺寸估计初版 | 建立误判样本记录表 | 形状/尺寸分类初版结果 |
| 7 月 7 日 | HDMI/OSD 显示 ROI、状态码或识别编号 | 实现 `task_mode`、`target_color`、`target_size`、`cube_only_guard` | 组织一轮完整识别不抓取测试 | 完整识别结果：形状、颜色、尺寸、match/action |
| 7 月 8 日 | 提供 CPU 与 FPGA 特征交换的稳定版本 | PC 调通抓取、放置、回安全位动作序列 | 记录点位、速度、失败原因 | PC 调试机械臂动作闭环 |
| 7 月 9 日 | 协助接入 CPU 触发信号和显示动作状态 | 板上 CPU 串口控制 myCobot 初版 | 录制 `grab/skip` 分支测试 | CPU 控制机械臂初版 |
| 7 月 10 日 | 修显示状态和触发互锁 | 完成 `match=yes` 抓取、`match=no` 跳过 | 做连续 5 次单项测试 | 单项连续 5 次运行记录 |
| 7 月 11 日 | 修复识别触发和重复触发问题 | 增加复位、安全位、动作超时处理 | 输出阶段 3 闭环报告 | 识别 + 判定 + 机械臂动作闭环 |
| 7 月 12 日 | 优化颜色阈值与光照兼容 | 优化动作速度和夹取容错 | 组织指定颜色分拣专项测试 | 指定颜色 5 次/轮测试记录 |
| 7 月 13 日 | 优化正方体识别与误判边界 | 优化抓取前等待和夹爪闭合参数 | 组织正方体 + 指定颜色专项测试 | 正方体颜色 5 次/轮测试记录 |
| 7 月 14 日 | 优化尺寸估计和 0.5 cm 区分 | 优化尺寸任务下的抓取判定 | 组织指定尺寸正方体专项测试 | 尺寸任务 5 次/轮测试记录 |
| 7 月 15 日 | 汇总识别阈值、资源、warning | 汇总点位、速度、动作耗时 | 做三项混合压力测试 | 三项任务均达到目标正确率 |
| 7 月 16 日 | 固化视频/识别工程版本 | 固化 CPU 程序、参数表、点位表 | 建立版本归档和回退包 | 主版本 bitstream/CPU 程序/参数包 |
| 7 月 17 日 | 提供资源占用、时序、关键 warning 解释 | 提供机械臂控制流程和异常处理说明 | 起草技术文档和 PPT 结构 | 文档证据包初版 |
| 7 月 18 日 | 协助录制清晰演示画面 | 协助录制稳定动作流程 | 录制三类评分任务演示视频 | 演示视频、测试数据、问题清单 |
| 7 月 19 日 | 只修阻塞问题，不做架构大改 | 只修安全/稳定问题，不改动作大逻辑 | 编写现场操作手册和应急预案 | 冻结候选版本、操作手册 |
| 7 月 20 日 | 现场前最终烟测 | 现场前最终烟测 | 全流程彩排和答辩串词 | 冻结版本、备用文件、最终彩排记录 |

## 5. 每阶段详细验收清单

### 阶段 1：硬件链路确认

- HDMI 采集卡可稳定显示 FPGA 输出画面。
- 记录摄像头输出分辨率、帧率、MIPI lane 数、RAW 位宽。
- 两个摄像头至少确认一个可稳定工作，另一个标记为备件或后续扩展。
- PC 能与 myCobot 通信，完成只读状态或安全小动作。
- 建立干净工程，不直接在赛方原始工程里做长期修改。

### 阶段 2：识别保底闭环

- FPGA 侧输出 ROI、面积、边界框、颜色统计、亮度统计等特征。
- CPU 侧能输出 `shape/color/size/match/action`。
- 指定颜色分拣默认使用 `color == target_color`。
- 指定形状 + 颜色使用 `shape == cube && color == target_color`。
- 指定尺寸使用 `shape == cube && size == target_size`。
- `cube_only_guard` 可打开/关闭。
- 针对不同环境光至少做明/暗/偏色三类样本测试。

### 阶段 3：机械臂闭环

- PC 调试动作与板上 CPU 正式动作分清楚。
- 板上 CPU 能发送 myCobot 控制命令。
- 动作序列包含安全位、抓取上方、下探、夹取、抬升、横移、放置、返回。
- 横向搬运距离大于 10 cm。
- 有动作互锁，避免同一识别结果重复抓取。
- 有复位、安全位、超时或失败处理。

### 阶段 4：评分项专项打磨

- 指定颜色分拣至少多轮 5 次测试。
- 指定形状 + 指定颜色分拣至少多轮 5 次测试。
- 指定尺寸正方体分拣至少多轮 5 次测试。
- 每次失败都记录原因：颜色误判、形状误判、尺寸误判、机械臂抓取失败、条件参数错误、显示/触发问题。
- 目标：三项任务每项稳定 80% 以上，冲刺 100%。

### 阶段 5：文档证据

- 保存 Efinity 构建日志、资源占用、关键 warning。
- 保存 CPU 程序版本、阈值参数、点位参数、动作耗时。
- 保存 HDMI 画面和机械臂动作演示视频。
- 技术文档包含系统方案、功能、实现过程、资源消耗、测试数据和风险处理。

### 阶段 6：冻结与应急

- 主版本冻结，不再做架构级改动。
- 准备备用 bitstream、CPU 程序、参数文件、线缆和供电。
- 准备应急模式：
  - 只显示识别不抓取。
  - 机械臂回安全位。
  - 重新加载参数。
  - 切换 `cube_only_guard`。
- 三名队员都能按操作手册启动系统，并解释自己的模块。

## 6. 每日同步模板

每天同步只回答 5 个问题：

1. 昨天交付了什么可验证产物？
2. 今天要交付什么可验证产物？
3. 当前最大阻塞是什么？
4. 是否影响 7 月 20 日主线？
5. 需要谁在今天配合？

## 7. 默认责任边界

- A 不能只交“画面能显示”，还必须交 CPU 可用的统计特征。
- B 不能只交“PC 能控制机械臂”，还必须迁移到板上 CPU 控制。
- C 不能只做文档，必须主持每天测试、记录失败原因、推动 A/B 修复闭环。
- 三人共同遵守：任何机械臂动作改动必须先空跑，再低速小幅，再带物块测试。
