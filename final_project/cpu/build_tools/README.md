# CPU Build Tools

本目录保存 CPU 构建和部署辅助脚本。

- `build.bat`：正式决赛 app 构建入口，转到 `cpu/app/` 执行 Makefile。
- `deploy.bat`：正式部署说明入口，待确认 Efinity Programmer/OpenOCD 流程后完善。
- `legacy_shape_detect/`：从初赛 `sw/` 复制的 shape_detect 构建基线，只作为迁移参考。
