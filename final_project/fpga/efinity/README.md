# Efinity 工程入口

本目录后续保存正式 `mem_test.xml`、`mem_test.peri.xml`、`constrain.sdc` 和 `debug_profile.wizard.json`。

落地规则：

- 先从赛方主 demo 复制工程文件。
- 再把源码和 IP 路径改为 `final_project/` 内相对路径。
- 路径调整和验证结果必须同步记录到 `docs/migration_log.md`。
