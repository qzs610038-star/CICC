# 技术文档修改提示词

## 使用方法

建议在新窗口中依次发送“提示词 1”至“提示词 5”。提示词 1 负责实际改稿，后四条用于独立复核和收口。不要一次性发送全部提示词，以免模型只给概括性建议而不落实到文件。

所有提示词共享以下底线：正文可以采用“系统已经设计并实现”的完成态语气，但不得虚构板级现象、测试次数、准确率、耗时、资源、时序、机械臂动作或实拍证据。没有证据的内容应改写为已实现的设计机制、接口约束或验收方法，不得伪装成已经通过的系统级测试。

---

## 提示词 1：主改稿任务

```text
请直接修改 D:\CICC w\技术文档.md，目标是把它改成一篇可用于第十届全国大学生集成电路创新创业大赛分赛区决赛的正式技术报告。不要只给建议，完成实际改稿；不要改动无关文件，不要覆盖工作区中已有的其他修改。

开始前必须完整阅读并遵守：
1. D:\CICC w\AGENTS.md
2. D:\CICC w\CURRENT_STATE.md
3. D:\CICC w\SESSION_HANDOFF.md，并运行 tools\agent_handoff_health_check.ps1；若脚本仅因系统没有 python 命令而失败，记录为工具环境缺失，不得据此推断 myCobot 通过或失败。
4. D:\CICC w\final_project\docs\competition_manual\第十届集创赛分赛区决赛雄芯院企业命题比赛细则_0710.md
5. D:\CICC w\.agents\skills\fpga_vision\SKILL.md
6. D:\CICC w\.agents\skills\cpu_mycobot\SKILL.md
7. D:\CICC w\CICC技术文档与PPT收集整理\01_第九届官方优秀作品专刊\优秀技术文档写作共性总结.md
8. 上述优秀作品专刊中的 04、06、10、11 四篇全文；其余七篇至少阅读摘要、总体方案、测试结果和结论。

代码定位优先使用 codebase-memory-mcp 的 search_graph、trace_path、get_code_snippet 和 search_code。代码图谱只用于定位，最终结论必须回到真实源码、工程 XML、日志、Review Packet 和 CURRENT_STATE.md。凡涉及固定 SHA、构建批次、板级状态、UART、APB、OSD 或机械臂的结论，必须核对真实证据。

一、总的写作目标

1. 全文采用正式技术论文语体，语气客观、克制、明确。以“本文设计”“系统采用”“模块完成”叙述已经实现的设计，不使用“计划”“拟”“未来将实现”等开发计划口吻。
2. “完成态语气”不等于可以虚构验收结果。设计完成、源码存在、离线仿真、Host 测试、综合构建、PC 端动作、板级闭环是六种不同证据层级，必须分别陈述，不得互相替代。
3. 删除明显的仓库治理口吻和内部工作记录口吻，例如正文中不应频繁出现 Gate、Review Packet、固定 SHA、bundle、A7、fail-closed、当前批次等词。确有复现价值的版本、SHA、hash 和 warning 明细移到“实现与验证环境”表或附录，不要占据摘要和结论的主要篇幅。
4. 读者应能从文章中回答五个问题：赛题要求什么；系统为何这样分工；各模块如何实现；如何验证；现有证据能支持什么结论。
5. 不要照抄优秀作品。借鉴其“问题—方法—实现—数据—结论”的组织方式，但所有技术内容必须来自本项目。

二、必须修正的事实与逻辑问题

1. 全文把“0715 修正版”改为仓库权威的“0710 比赛细则”，包括现有第 29、183、353 行附近。若确有另一份 0715 文件，只有在找到原文件并核对正文后才能引用；不能凭旧文档名称推断。
2. 摘要现有“软硬件模块均通过对应的功能检查”范围过大。改为准确分层的结果句：Efinity 冷构建完成 Map、Interface、PNR、STA、CDC 和 bitstream 生成；CPU Host 测试为 182/182，Python 离线回归为 3/3。不得把这些写成板上 CPU、APB、CPU→OSD、UART2/J52 或 myCobot 闭环已通过。
3. 明确区分 final_project 正式主线与 competition_project_single_camera 隔离候选。单摄 Hard SoC 的冷构建证据来自哪个工程、哪个不可变输入批次，就只支撑该工程和批次；不得把候选工程的构建结果写成 final_project 整机闭环完成。
4. 当前状态明确未验证 USER2、CPU 实际取指、UART0 115200 横幅/回显、APB 实读、正式 MMIO ABI、CPU→OSD、正式目标输入、逐轮板级事务、UART2/J52 和机械臂板控闭环。正文可以描述这些模块的实现结构和协议，但不能用“实测表明”“已经打通”“稳定运行”等措辞。
5. 第 4.4 节的 5/5 机械臂结果是 PC 端独立路径测试，只能证明点位顺序、运动方向和 PC 调试流程；不能证明板上 CPU 控制、UART2/J52、目标识别触发或端到端分拣。标题应改成“PC 端动作路径与点位验证”或等价准确名称。
6. 177.72°是由抓取点与放置点坐标推导的方位变化，2.02°是某阶段关节软到位误差，两者不能混作官方“旋转 180°±10°”和“最大臂展处放置”的完整验收证据。需要分别说明测量对象、计算方法和局限。
7. `ARM_NOT_READY`时不动作属于安全降级，不是满足比赛执行要求。写清楚：它避免盲动，但目标物未执行会失去该轮执行分，不能把安全降级写成任务成功。
8. 图 3-4 是 OSD 版式示意，不是板级实拍。保留明确图注。没有真实板级截图时，不得在结果章节暗示 OSD 已通过现场显示验证。
9. 技术文档_assets\figure_09.png 写明 APB 正式地址和 MMIO ABI 尚未冻结，figure_12.png 写明板级同批次未验证。这两张图属于内部证据边界说明，不应加入完成态参赛正文；除非后续证据已经改变，并同步重绘图中文字。
10. UART0 为 115200，myCobot 为 1000000，两条链路必须始终分开写，不能用 UART0 的离线 ELF 或未来 Hello 结果外推 myCobot 链路。

三、必须补上的技术漏洞分析

1. 颜色识别：说明红、蓝、黄面积阈值、白黑亮度判定的输入量和判决次序；指出光照、白平衡、阴影和高光会影响阈值。给出参数标定方法、未知类别条件和置信边界。没有样本数据时不要编造准确率。
2. 形状识别：仅用 `foreground_area / bbox_area` 区分正方体、圆柱体和锥体存在视角、姿态、遮挡和投影相似风险。说明拍摄平面、相机俯视角、物体姿态和 ROI 的固定条件；增加“低置信度输出未知/不执行”的规则。若源码还有宽高比、轮廓或其他特征，应核对后补入；不得凭空新增算法。
3. 尺寸识别：bbox 像素高度受透视、距离和摆放位置影响。说明相机与台面的固定关系、2/2.5/3 cm 三点标定、阈值中点、误差容限和越界处理。严格区分“尺寸分类结果”和“尺寸关系判断”。
4. 任务三：参考尺寸只能为 2 cm 或 3 cm，目标分别是 3 cm 或 2 cm；五个物体中目标数为两个。任务四：目标尺寸为 2 cm 或 3 cm，满足差值不超过 0.5 cm 的物体包括目标尺寸本身和 2.5 cm，因此五个物体中通常有三个目标。检查状态机是否支持一个任务中多轮目标，而不是把“目标锁”错误地跨轮保持。
5. 快照一致性：`frame_id/ACK`能防止读取撕裂，但必须核对 ACK 的时钟域、跨域同步、旧 ACK 清除新快照的条件和 frame_id 回绕。若当前只在模块仿真中验证，就写“模块级协议实现”，不要写成板级 CDC 已验证。
6. 配置一致性：核对 staging/commit/active 是否确实存在于 RTL、CPU 和顶层连线。只有接口契约或 CPU 侧函数而没有同批次顶层接线时，不能说“保证 CPU→FPGA 原子更新”。
7. 动作唯一性：说明事件序号、轮次锁和 `arm_request_sent`各自防止什么重复；检查 START 长按、按键抖动、重复帧、迟到响应、END 误触、超时后迟到帧、frame_id 回绕等边界。
8. myCobot：核对帧长定义、命令 ID、大小端、角度单位和响应长度的官方来源版本。区分“串口字节已发送”“响应匹配”“关节到位”“夹爪闭合”“物体夹牢”五种状态。位置反馈不能单独证明夹稳。
9. 官方动作验收：报告中必须覆盖轻取轻放、提前释放/跌落判定、旋转 180°±10°、最大臂展放置、动作唯一性和故障停机；没有板级实测时只能写设计约束与验收方法，不能给通过结论。

四、建议重构后的章节

可在保留已有素材的基础上改为以下结构，不必机械照搬章节名：

摘要：问题、架构、三项具体方法、已有量化结果、证据边界。控制在 300—450 字，避免两段长排比。

第 1 章 绪论：赛题需求；现有纯 FPGA、纯 CPU/完整帧搬运方案的不足；本文工作与贡献。贡献限定为三条，每条都写“做了什么、解决什么、由什么证据支撑”。不要使用“首次”“领先”“显著”除非有检索或对比数据。

第 2 章 系统需求与总体架构：增加“赛题要求—系统功能—实现位置—输出—验证方式”总表；补充硬件平台、摄像头、显示链、Ti375、片上 RISC-V、myCobot 280、串口和操作输入的基本信息。未知参数保留为待核实项，不得猜测。

第 3 章 FPGA 视频前端与帧级特征：按“设计约束—2PPC 数据组织—掩码与累计—快照—接口与 CDC—模块仿真”展开。把大段 Verilog 代码改成公式、数据流图或伪代码；正式技术报告不应像源码说明书。

第 4 章 CPU 分类、任务判断与轮次事务：分别说明颜色、形状、尺寸；四任务真值条件；START/END事务；结果语义；异常与降级。增加任务三、四的真值表示例和原因码示例。

第 5 章 OSD 与机械臂执行：先说明 CPU 产生语义、FPGA 渲染；再说明 myCobot 协议事务、动作状态机、点位与安全条件。明确 PC 只用于开发期点位验证，不进入正式闭环。

第 6 章 实现与验证：分成 RTL 仿真、CPU Host 测试、Efinity 构建、PC 动作路径验证、系统级验收五层。每层写测试对象、输入、判据、结果、不能证明什么。系统级验收没有真实证据时，不得生成成功数字，可给出验收矩阵但不要勾选通过。

第 7 章 结论：只总结已由证据支撑的实现和指标；指出一个具体工程限制，例如系统准确率和端到端耗时仍需同批次板级样本统计。结论 150—250 字，不重复整篇目录。

附录：版本与证据索引、寄存器逻辑分组、测试命令、warning 分类和团队分工。SHA、hash、内部 Review Packet 放这里比放摘要更合适。

五、必须新增或改造的表格

1. 赛题要求与实现映射表：四任务、识别输出、判断条件、目标响应、非目标理由、实现位置、验证层级。
2. 硬件与软件平台表：器件/版本/接口/用途。只填真实可查项。
3. 分类特征与判决表：颜色、形状、尺寸各自输入特征、判决方法、未知条件、主要误差来源。
4. 四任务真值表：尤其写清任务三和任务四在参考/目标为 2 cm 或 3 cm 时哪些尺寸应抓取。
5. 分层验证结果表：RTL、Host、构建、PC 动作、板级闭环分别列出证据。板级闭环缺证据时标为“本文无可引用实测数据”，不要写 PASS。
6. Efinity 表增加时钟域、约束周期或目标频率，否则单独给 WNS/WHS 缺少解释背景；资源项给出百分比并核对分母含义。Inputs/Outputs/XLRs 若是路由资源而非器件 IO，必须改成准确名称。
7. 系统级指标表预留但不编造：五色/三形/三尺寸混淆矩阵、每类样本数、识别准确率、目标判断准确率、抓取成功率、跌落次数、单轮耗时、20 轮总耗时、180°放置误差、最大臂展位置偏差。

六、图片要求

1. 第一张图保留为系统总体架构，但需标清真实数据流、控制流和验证边界；不要用一个连续箭头让读者误以为所有接口都已板级打通。
2. 保留 2PPC、快照、轮次状态机、myCobot事务和动作状态机等关键图，统一字体、箭头方向、图题和中英文缩写。
3. OSD示意图必须始终标为“版式示意”；实拍只有在确有原始板级照片时才能替换。
4. 增加真正有信息量的结果图：RTL 波形、资源占用图、测试结果统计图、机械臂路径/坐标示意。没有原始数据时不要画漂亮但虚假的曲线。
5. 每张图都要在正文中先引用再出现，并写一句“图中能看出什么”。清理未引用资产或明确其内部用途，但不要擅自删除用户文件。

七、语言与格式要求

1. 去掉 AI 常见套话：不要写“赋能”“打造”“全面提升”“具有重要意义”“为……提供有价值的参考”“三项核心设计贯穿全文”“既……又……同时……”“显著”而无数据。
2. 不用连续三段相同句式，不堆叠“首先、其次、最后”，不把三四个抽象名词并列成口号。
3. 每个技术结论尽量包含对象、条件、动作和结果。例如：“在快照 valid 保持期间，模块拒绝覆盖新帧统计，并递增 dropped_frames；该策略保留字段一致性，但会丢弃中间帧。”
4. “保证”只用于可由结构、断言或测试证明的性质；其余改为“用于避免”“降低……风险”“在……条件下成立”。
5. 中文与英文、数字与单位按统一规范排版：2 PPC 或 2PPC 选一种并全文统一；32 bit/32-bit 统一；数值与单位之间留空格；公式变量用斜体语义，首次出现解释。
6. 减少正文中的反引号、路径、函数名和源码片段。只有理解机制必需时才保留函数名；其余移到附录。
7. 参考文献按统一标准补齐作者/机构、题名、文献类型、版本或发布日期、访问日期。内部计划、Review Packet 与寄存器表改列为“工程证据”或附录索引，不与正式公开文献混排。
8. 标题中的“具身智能”和“芯机协同”可能被质询为概念拔高。若队伍作品名称尚未锁定，建议改成“基于国产 FPGA 与片上 RISC-V 的视觉分拣机器人系统设计”一类可由内容直接支撑的名称；若名称已提交，则在绪论中用一句话给出严格定义，避免把规则状态机包装成通用具身智能。

八、交付要求

1. 直接完成 D:\CICC w\技术文档.md 的修改。
2. 修改后给出“关键改动清单”和“仍缺原始证据清单”，逐项标明对应章节。
3. 扫描全文，列出所有数字及其证据路径；没有证据路径的结果数字不得留在正文。
4. 检查所有图片路径存在、所有图片均被正文引用、图表编号连续、参考文献均在正文出现。
5. 不修改 CURRENT_STATE.md、AGENTS.md、官方比赛细则、源码、工程 XML、SDC、IP 或机械臂脚本。
```

---

## 提示词 2：证据与漏洞红队复核

```text
请对刚修改的 D:\CICC w\技术文档.md 做一次独立的“评委追问式”红队审查，并直接修正发现的问题。不要只做语言润色。

逐条检查：
1. 每一个“实现、完成、通过、稳定、保证、准确、实时、闭环”后面是否有明确对象、条件和证据。
2. 是否把源码存在写成顶层已接入，把仿真通过写成上板通过，把 Host/fake transport 写成 RISC-V/MMIO 通过，把 Efinity 构建写成板级通过，把 PC 动作写成板上 CPU 控制通过。
3. 是否混用了 final_project 与 competition_project_single_camera 的证据。
4. 是否把 UART0 115200 与 myCobot 1000000 混成一条链路。
5. 是否暗示 APB 实读、CPU→OSD、正式目标输入、逐轮板级事务、UART2/J52 或机械臂板控已经验证。
6. 任务三、四的目标集合是否正确；任务四是否漏掉与 2 cm/3 cm 目标相差 0.5 cm 的 2.5 cm 正方体。
7. 颜色、形状、尺寸算法在光照、视角、透视、摆放位置和低置信度条件下是否有明确处理，是否存在“一定能识别”的过度承诺。
8. `frame_id/ACK`、staging/commit/active、事件序号、动作单发分别解决的问题是否混淆；CDC 和回绕问题是否被无条件忽略。
9. 机械臂 177.72°、2.02°、5/5 分别测量什么，是否被误写成官方整机验收。
10. WNS/WHS 是否带有构建批次和时钟约束背景；warning 是否被简化成“无风险”。
11. 摘要和结论是否超出正文证据；参考文献中的 0710/0715 是否一致。

输出时先列按严重度排序的问题及行号，然后直接修改文件。修正原则是缩小结论范围、补条件、补证据层级，不是用含糊词掩盖问题。严禁创造任何测试数据。
```

---

## 提示词 3：去 AI 味和学术表达复核

```text
请对 D:\CICC w\技术文档.md 做一次逐段人工化改写，目标是像参赛队员在理解系统后写出的严谨技术报告，而不是模型生成的项目总结。直接修改文件。

重点处理：
1. 删除空泛开场、口号、拔高价值和无数据形容词。
2. 拆掉过长排比句；一句只承担一个主要判断，必要时用两句解释因果。
3. 避免连续使用“通过……实现……从而……保证……”，改成明确的输入、处理、输出和限制。
4. 避免每节末尾都用“三点总结”或“这说明……具有……意义”。
5. 把源码说明书式写法改成论文写法：少列函数名和代码，多写数据组织、状态转移条件、公式和边界。
6. 术语首次出现时给出中文全称和英文缩写，后文统一；不要反复中英混排。
7. “具身智能、芯机协同、原子协议、事务、稳定、实时、低带宽、无回压、最大臂展”等词必须有严格定义或量化条件。
8. 不回避代价：快照保持会丢帧、规则分类依赖标定、Host 测试不覆盖 MMIO、PC 动作不等于板控。用一两句技术性限制替代大段自我辩护。
9. 摘要控制在 300—450 字，结论控制在 150—250 字，二者避免逐句重复。
10. 保持客观第三人称，不使用“我们认为”“不难看出”“显而易见”“完美解决”。

完成后搜索并报告以下词语的剩余位置及保留理由：显著、保证、完整闭环、稳定运行、实时、创新性、首次、领先、赋能、打造、全面。
```

---

## 提示词 4：测试、图表和结果章节补强

```text
请专门审查并修改 D:\CICC w\技术文档.md 的“实现与验证”部分，使其达到优秀作品常见的量化、可复现水平，但绝不编造数据。

要求：
1. 建立分层验证矩阵：RTL模块仿真、CPU Host/fake transport、Efinity 冷构建、UART0 ELF 静态审计、PC 端点位动作、板级端到端。每层列测试对象、工具与版本、输入、判据、结果、证据路径、不能证明的事项。
2. 核对 182/182、3/3、20 个确定性事务、WNS 1.321 ns、WHS 0.026 ns、bitstream/ELF hash、5/5、177.72°和2.02°的原始证据。找不到证据的数字从正式结论中删除或标为待核实，不得沿用二手摘要。
3. 对 Efinity 结果增加时钟约束背景、资源百分比和 warning 分类。不得用“CDC PASS”替代对跨域结构的解释。
4. 把 PC 端 5/5 动作结果放在独立小节，明确它不覆盖识别触发、板上 UART2/J52 和完整分拣闭环。
5. 增加“赛题系统级验收指标”表。只有真实原始数据才能填结果；缺少数据时保留测量方法和判据，不填写漂亮数字。
6. 检查每张结果图是否来自真实波形、日志或照片。架构图、版式示意图不得放在“实验结果”中冒充实测图。
7. 结果讨论必须解释数据而非复述表格，包括取舍、误差来源、适用条件和未覆盖项。
8. 若 D:\CICC w\技术文档.docx 已存在并需要同步，先确认它是否由 Markdown 生成；未经确认不要让两个版本产生内容分叉。

直接修改 Markdown，并在回复中列出“已核实结果”“仅设计/离线证据”“仍缺系统实测”三张清单。
```

---

## 提示词 5：最终交付检查

```text
请把 D:\CICC w\技术文档.md 当作即将提交给评委的最终技术报告，做最后一次全量检查并直接修正。

验收项：
1. 官方规则、任务数量、轮数、分值、目标定义、180°±10°、最大臂展和跌落定义与 0710 原文一致。
2. 摘要、正文、表格、图题、结论和参考文献之间没有互相矛盾。
3. 所有完成态表述都有真实实现或证据支撑；没有用将来时掩盖缺失，也没有虚构完成结果。
4. final_project 与单摄候选、离线与板级、UART0 与 myCobot、PC 调试与正式闭环边界清楚。
5. 章节结构完整，图表先引用后出现，编号连续，所有链接和图片路径有效。
6. 每个表头带单位或在正文解释单位；每个公式解释变量；每个缩写首次出现时解释。
7. 参考文献格式统一、来源完整，内部工程证据与公开文献分开。
8. 无学校、导师、个人姓名等盲审信息；队号和团队名称是否保留以官方模板要求为准。
9. 全文没有“AI味”套话、重复总结、过度宣传、模糊主语和无条件保证。
10. 给出一份不超过 20 项的最终风险清单，按“会被评委当场追问的概率”排序。只列尚未解决的真实风险。

最后输出：修改文件路径、章节清单、核实过的关键数字及其证据路径、仍不能声称通过的项目。不要再生成一份平行的新文档。
```
