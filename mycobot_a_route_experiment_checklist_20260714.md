# myCobot 280 A 路线正式实验执行清单 (2026-07-14)

本文件是 myCobot 280 在 PC 端通过 Python 脚本进行真机示教与受控诊断的正式执行清单。本清单已集成最新状态决策（跳过 E3 物理测定，直接进入真机示教 E4，以实际抓取结果反推误差）。

> [!IMPORTANT]
> **本实验仅用于 PC 端离线示教与诊断验证，所生成的点位及日志作为后续板上固件移植的参考依据。**
> 任何 PC 端的 Python 控制代码均不进入正式的 FPGA/CPU 识别与控制闭环。

---

## 1. 物理安全与准备前提

在启动任何命令前，现场必须严格闭环以下安全措施：

1. **安全员在场**：必须有至少一名安全员站在机械臂旁，**手持 12V 独立电源插头**。
2. **断电响应要求**：一旦发生轨迹异常、碰撞或异常发热，安全员必须在 **2 秒内** 拔掉电源插头。
3. **急停优先级**：**物理断电（拔电源） > 软件急停（Ctrl+C）**。Ctrl+C 仅发送软件 `stop` 命令，在固件卡死或通信中断时无法制动。
4. **风险边界定义**：本次实验中，**抓取失败是可接受的，但碰撞、夹爪损坏和舵机过载是不可接受的**。
5. **环境净空**：确保机械臂运动半球范围内没有任何杂物、水杯或贵重仪器，基座已放置在稳固平面上。

---

## 2. 实验步骤与命令序列

本实验采用**三步递进**策略。**前一步骤未顺利通过并完成日志审计前，严禁进入下一步骤。**

本实验统一使用 `mycobot_pc_tests/teach_replay_pick_return_ready.py` 脚本，该脚本完美兼容预设 JSON 导入、单段诊断和完整抓放。

### 步骤 A0：真机手动示教（获取全新预设点位）

在运行诊断前，必须先释放舵机，手动拖动机械臂录入 5 个关键点位并保存。

* **执行命令**：
  ```powershell
  python mycobot_pc_tests/teach_replay_pick_return_ready.py --teach-only --save-preset route_a_20260714_2000 --port COM9
  ```
* **操作规程**：
  1. 运行命令后，程序会提示确认安全门。确认后，机械臂所有舵机将释放动力。
  2. 手动将机械臂拖动到以下 5 个位置，并按终端提示回车记录：
     * **`pick_hover`**：抓取点正上方悬停点（Z 轴抬高以净空，推荐高度 ~70mm 以上）。
     * **`pick`**：实际抓取目标位置（夹爪应能够刚好罩住物块）。
     * **`drop_hover`**：释放点正上方悬停点。
     * **`drop`**：实际释放目标位置（位于相对起点旋转 170°~190° 的最大臂展平面内）。
     * **`home_ready`**：空中过渡中间态（返回直立零位的安全中转点，arm_max_diff 必须 $\le 40^\circ$ 以留足自动回零余量）。
  3. 示教完成后，点位将自动保存为 `mycobot_pc_tests/presets/teach_points_route_a_20260714_2000.json`。
* **准入下一阶段判定**：
  * 终端提示 `[PASS] 候选点位已保存`。
  * 后续步骤 A1 运行后，程序在串口握手成功后会自动先运行 `=== 预设安全门校验 ===`，若无报错拦截即说明点位及过渡角度均合规。

---

### 步骤 A1：单段 hover 诊断（HOME ↔ pick_hover 往返）

验证机械臂从直立零位移动到抓取上方悬停点并读回的轨迹与精度，夹爪不动作。

* **执行命令**：
  ```powershell
  python mycobot_pc_tests/teach_replay_pick_return_ready.py --probe-pick-hover --preset route_a_20260714_2000 --port COM9
  ```
* **预期现象**：
  1. 机械臂上电抱紧。
  2. 从直立 HOME 慢速移动到示教的 `pick_hover` 点。
  3. 读回当前关节角度与空间坐标，打印对比误差。
  4. 慢速原路返回 HOME 姿态。
* **审计与判定**：
  * 查看 `mycobot_pc_tests/audit_logs/auto_run_<时间戳>.log` 审计日志。
  * 检查最大关节误差是否 $\le 2^\circ$，空间物理残差是否在合理范围内。
  * 检查基座在运动过程中是否有物理位移。
  * **通过**：进入 A2。
  * **失败**：重新调整物理基座或重新运行 A0 示教。

---

### 步骤 A2：下探诊断（HOME ↔ pick_hover ↔ pick 往返）

验证下探到抓取点的轨迹安全性，夹爪保持张开，不碰夹爪。

* **执行命令**：
  ```powershell
  python mycobot_pc_tests/teach_replay_pick_return_ready.py --probe-pick --preset route_a_20260714_2000 --port COM9
  ```
* **预期现象**：
  1. 机械臂从 HOME 移动到 `pick_hover`。
  2. 从 `pick_hover` 垂直下探至 `pick` 抓取点。
  3. 读回并打印 `pick` 点的关节角及坐标数据，输出物理残差。
  4. 向上抬起回到 `pick_hover`，随后慢速返回 HOME。
* **审计与判定**：
  * 检查 `pick` 点到位状态。在 `[受控探针 pick 状态数据]` 部分核对目标坐标与实际读回坐标。
  * 若各轴最终绝对差 $\le 2^\circ$，且未发生关节抖动或卡死，可判定此轨迹安全。
  * **通过**：进入 A3。
  * **失败**：微调 `pick` 点，重跑 A0 示教及 A1、A2。

---

### 步骤 A3：完整抓放自动测试

执行带夹爪控制的完整抓取、空中移动、释放及中转回零测试。

* **执行命令**：
  ```powershell
  python mycobot_pc_tests/teach_replay_pick_return_ready.py --preset route_a_20260714_2000 --port COM9
  ```
* **预期现象**：
  1. 终端提示输入“连续抓取次数”，**输入 `1` 并回车**。
  2. 程序提示确认物块就位后，按回车开始。
  3. 夹爪完全张开 $\rightarrow$ 移动至 `pick_hover` $\rightarrow$ 下探至 `pick` $\rightarrow$ 夹爪闭合抓取。
  4. 抬起至 `pick_hover` $\rightarrow$ 空中大范围过渡到 `drop_hover` $\rightarrow$ 下下降至 `drop` $\rightarrow$ 夹爪张开释放。
  5. 抬起至 `drop_hover` $\rightarrow$ 移动到中间点 `home_ready` $\rightarrow$ 自动安全回零直立状态。
* **审计与判定**：
  * 检查物块释放角度是否位于起点旋转 180°（正负 10° 范围内，即 170°~190°）。
  * 检查物块是否发生了提前释放或半空跌落。
  * 检查自动回零是否顺利完成，未触发“人工扶正”软拦截。

---

## 3. 结果判断与分流决策树

抓放结束后，必须根据下表对测试结果进行分类，并决定后续路径：

| 现象分类 | 诊断结论 | 后续动作决策 |
|:---|:---|:---|
| **完全成功** | 抓放精确，轨迹平滑，无碰撞，自动回零成功 | 1. 登记当前点位为 A 路线黄金点位。<br>2. 可重复执行 A3（例如输入 3 次）测试稳定性。<br>3. 提取预设 JSON 用于板端点表。 |
| **抓放失败，但轨迹正常**<br>*(如未夹稳滑动、未放正、夹爪力矩不足)* | 示教点位大体正确，但微观参数（如下探高度、夹爪延时/速度、释放坐标）需要微调 | 1. 微调预设 JSON 里的少量坐标或在示教时略微改变下探位置。<br>2. 重新从 A0 开始录制并依次执行 A1 $\rightarrow$ A3。 |
| **抓放失败，且轨迹异常**<br>*(如关节大幅摆动、限位越界、出现机械碰撞)* | 底座固定不稳、原点漂移或示教点落入不同逆解分支 | **立即停止测试！**<br>1. 重新检查底座的刚性固定。<br>2. 回头执行 E3 机械前置门进行精确误差测定。<br>3. 严禁再次盲目启动 A3 抓放。 |

---

## 4. 日志审计核查清单

每次测试结束后，请确认 `mycobot_pc_tests/audit_logs/auto_run_<时间戳>.log` 日志中包含以下关键证据：
- [ ] 头部带有 `[V2.8 日志] 终端输出同步写入`。
- [ ] 记录了正确的 `MyCobot` 串口连接参数（波特率 `1000000`）。
- [ ] 在 `[受控探针]` 阶段包含 `各轴最终绝对差` 及 `最终空间物理残差`（正常应 $\le 2.0^\circ$ 和 $\le 20\text{ mm}$）。
- [ ] 返回 HOME 时未出现 `[警告] 自动回零失败，触发人工扶正`。
