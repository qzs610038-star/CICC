# “雄芯院”企业命题技术设计文档大纲 V2

> 作品名称：具身智能机器人芯机协同多场景目标感知与自主操控系统设计
>
> 建议副标题：基于国产 FPGA、片上 RISC-V 与 myCobot 的视觉分拣系统
>
> 文档属性：分赛区决赛盲审技术设计文档
>
> 建议篇幅：正文 24-28 页，附录 4-8 页，最终提交 PDF
>
> 写作口径：按完整目标方案展开；结果、资源和完成度必须使用当前匹配证据，不以计划替代实测。

## 已确认的交付与系统口径

- 截止时间：2026年7月20日23:59。
- 交付格式：DOCX；PDF由团队自行转换。
- 队伍编号：CICC1002207。
- 姓名策略：封面可出现，正文原则上不出现；未取得最终名单前保留占位符。
- 正文主线：单摄像头 + Ti375 FPGA + 片上RISC-V CPU + myCobot 280。
- 目标配置：拨码输入。
- 结果主通道：HDMI OSD。
- 当前可确认现象：摄像头到HDMI链路可运行；机械臂可独立工作。
- 当前待队友证据：FPGA特征到CPU、CPU决策到OSD及整机闭环，未收到证据前不写成已跑通。

## 0. 写作总则

### 0.1 评分对齐

技术文档对应 25 分，必须直接覆盖：

1. 系统方案。
2. 系统功能。
3. 实现过程。
4. FPGA 逻辑资源、存储资源和 DSP 消耗。
5. 可复核的测试、构建和板级证据。

### 0.2 状态标签

统一使用以下四类标签：

- `已验证`：有当前匹配批次执行日志、构建报告或板级现象。
- `Host 已验证`：只证明算法、状态机或接口 seam 的离线行为。
- `设计已冻结/待板测`：方案和契约已明确，但没有板级闭环证据。
- `计划/待验证`：完整方案中的目标能力，不写成已经完成。

### 0.3 盲审与事实红线

- 不出现学校、导师、队员姓名、Logo；实物照片中的标签也需脱敏。
- 不写无证据的准确率、毫米级误差、单轮耗时、功耗或抓取成功率。
- UART0 Hello 使用 `115200`；myCobot 链路使用 `1000000`，两者不得混写。
- `competition_project_single_camera/` 是单摄 Hard SoC 候选，不能不加说明地替代 `final_project/` 正式主线。
- Host、RTL 仿真、Efinity 构建和板级闭环四类证据必须分列。

## 1. 建议页数与章节安排

| 部分 | 建议页数 | 评委读完应得到的唯一结论 |
|---|---:|---|
| 封面、摘要、目录、速查表 | 4 | 作品解决什么问题、采用什么架构、当前有哪些可信结果。 |
| 第1章 赛题需求与设计目标 | 2 | 系统设计逐条对应四任务、20轮和三级评分链。 |
| 第2章 系统总体方案 | 3 | FPGA处理高带宽像素，CPU处理复杂决策，形成板内闭环。 |
| 第3章 FPGA视觉感知设计 | 4 | 不依赖外部PC，在视频流旁路完成低带宽统计特征提取。 |
| 第4章 RISC-V决策与逐轮事务 | 4 | 四任务规则、结果语义和唯一动作由板上CPU统一管理。 |
| 第5章 机械臂控制与安全 | 3 | 动作链路采用有界事务、互锁和故障收敛，避免重复触发。 |
| 第6章 芯机接口、CDC与OSD | 3 | 帧快照、配置和显示结果均以原子提交保证一致性。 |
| 第7章 实现、验证与资源 | 4-5 | 结论按证据层级呈现，资源数据来自匹配Efinity批次。 |
| 第8章 创新点、总结与展望 | 2 | 三项创新均有问题、方法、实现落点和验证路线。 |

## 2. 前置页面

### 封面（1页）

- 正式题名沿用企业命题名称。
- 可在题名下加入副标题，不改变命题范围。
- 仅保留杯赛名称、队伍编号和日期。

### 摘要与关键词（1页）

摘要采用“问题-方案-实现-证据”四句结构，控制在 250-350 字。结果句只填正式证据表中可复核的数据。

建议关键词：国产 FPGA；Ti375；芯机协同；机器视觉；RISC-V；自主操控。

### 目录（1页）

目录只列到二级标题，保证评委能够快速定位 FPGA、CPU、机械臂、资源和测试章节。

### 关键指标与证据速查（1页）

建议表头：`指标/功能 | 官方要求 | 完整方案 | 当前证据层级 | 文档章节`。

必须列出四项任务、五色三形状三尺寸、20轮、10分钟、180度放置、资源、时序和安全状态。

## 3. 第1章 赛题需求与设计目标（2页）

### 1.1 应用背景与核心问题

核心结论：系统面对的不是单一分类问题，而是受时限、随机顺序、动作唯一性和可解释输出共同约束的“感知-决策-操控”闭环。

禁止写大段 FPGA、RISC-V 或具身智能科普。

### 1.2 官方任务与评分链

用一张表说明四任务：

- 指定颜色正方体。
- 混合形状池中的指定颜色正方体。
- 与 2 cm/3 cm 参考边长差等于 1 cm。
- 与 2 cm/3 cm 目标边长差不超过 0.5 cm。

用流程图说明每轮 `识别25% -> 判断25% -> 执行/不执行50%`，强调前级错误使后级不得分。

### 1.3 系统设计目标

- 摄像头由指定 FPGA 直接控制。
- 正式识别和控制闭环不依赖 PC。
- 每轮输出颜色、形状、尺寸、目标判断、动作及理由。
- 目标轮轻取轻放至旋转 180 度（正负10度）最大臂展处。
- 非目标轮保持零动作并说明原因。

证据入口：官方 0710 比赛细则、`赛题要求提取.md`。

## 4. 第2章 系统总体方案（3页）

### 2.1 系统总体架构

必须绘制全文最重要的架构图：

`MIPI摄像头 -> FPGA视频前端/特征统计 -> APB快照 -> 片上RISC-V分类与任务判断 -> OSD结果/机械臂命令 -> myCobot`

图中使用三种边框区分 FPGA RTL、CPU C 固件和物理外设；使用状态角标区分已验证、Host已验证和待板测。

### 2.2 芯机职责划分

| FPGA | 板上CPU |
|---|---|
| MIPI/RAW、Debayer、Gamma、ROI、统计特征、OSD像素渲染、APB/UART硬件通道 | 五色三形状三尺寸分类、四任务关系判断、逐轮事务、参数、结果语义、myCobot协议与动作状态机 |

论证重点：像素级并行计算留在 FPGA；低带宽、规则复杂且需快速调整的控制逻辑留在 CPU。

### 2.3 完整工作流程

建议用泳道图展示：任务配置、放置、帧快照、识别、判断、OSD确认、目标动作/非目标跳过、移除和下一轮。

证据入口：`final_project/README.md`、当前代码架构文档、决赛主方案。

## 5. 第3章 FPGA视觉感知子系统（4页）

### 3.1 MIPI采集与视频预处理

说明 RAW 解包、Debayer、白平衡/Gamma 和显示链路。重点写数据格式、双像素通路和流水化处理，不写工具操作教程。

### 3.2 ROI与轻量颜色特征

说明通过通道差值、亮度门限和面积统计形成红、蓝、黄及黑白判别所需特征，避免在 FPGA 中实现高代价完整分类状态机。

### 3.3 无DDR、无回压的旁路统计

这是创新点一的主体：

- `feature_stats_tap` 只读监听视频流。
- 不向显示主链路施加 ready/backpressure。
- 每周期处理 Even/Odd 两个 RGB888 像素。
- 累计颜色面积、前景面积、bbox、中心点和亮度/通道和。
- 帧边界锁存稳定快照，不搬运完整图像到 CPU。

建议图表：2PPC数据通路图、帧内累计/消隐锁存时序图、特征字段表。

### 3.4 OSD像素渲染与显示输出

区分 FPGA 实时框和 CPU 回写稳态语义；说明 OSD 只负责渲染，不在 RTL 内重新作任务判断。

证据入口：FPGA RTL、`vision_module_implementation_plan.md`、单摄 feature contract。

## 6. 第4章 RISC-V决策与逐轮事务（4页）

### 4.1 多特征分类

- 颜色：红/蓝/黄面积显著性与黑白亮度/填充率判定。
- 形状：bbox长宽、前景面积和填充率。
- 尺寸：完整方案采用标定后的像素尺度或双视角高度特征；没有实测标定时必须标记待验证。

建议给出关键判定公式和阈值表，不贴大段源码。

### 4.2 四任务统一关系判断

以统一 `observation + target_config -> match_action + reason` 契约覆盖四任务，重点展示任务三/四是参考尺寸关系判断，不是简单固定尺寸匹配。

建议图表：四任务真值表、理由码表。

### 4.3 面向评分链的一轮一事务状态机

这是创新点三的主体。状态图至少包含：

`CONFIG -> WAIT_PLACE -> OBSERVE -> IDENTIFY -> DECIDE -> DISPLAY -> ARM_WAIT/SKIP -> WAIT_REMOVE -> COMPLETE/FAULT`

必须写明以下不变量：

- 同一物体只锁存一次识别结果。
- 同一轮最多发出一个机械臂请求。
- 非目标轮绝不动作。
- 新帧不得覆盖已锁存轮次结果。
- 超时、放弃和复位均有明确出口。

### 4.4 参数与结果语义

说明目标颜色、任务模式、参考尺寸、阈值页、错误码和理由码的管理方式。

证据入口：CPU分类器、task matcher、round controller、competition transaction及其Host测试。

## 7. 第5章 机械臂控制与安全（3页）

### 5.1 myCobot协议与链路

说明 `FE FE LEN CMD ... FA` 帧结构、1000000波特率及调试UART隔离。协议无事务序号，因此上层必须执行单请求在途和响应语义匹配。

### 5.2 非阻塞动作状态机

说明接近、夹取、抬升、旋转/搬运、放置、撤离和回到安全位的分阶段控制。动作完成依据到位反馈和稳定窗口，不使用无界死延时。

### 5.3 安全互锁与故障收敛

- 动作前检查目标轮、系统状态、点位和通信状态。
- 有界超时、有限重试、迟到/重复帧丢弃。
- 故障进入安全停机，不以盲复位继续动作。
- 急停/断电、机械固定和低速验证作为独立板级Gate。

建议图表：动作状态机、协议事务时序、安全Gate表。

证据入口：`mycobot_protocol`、`mycobot_transaction`、`arm_controller`和板控计划。

## 8. 第6章 芯机接口、CDC与可解释OSD（3页）

### 6.1 APB轻量寄存器接口

说明为何低速配置和统计快照使用APB，而不是让CPU读取整帧或引入复杂AXI突发。

寄存器按三类组织：`feature_regs`、`result_regs`、`control_regs`。基址必须来自同批次 `soc.h`，正文以偏移和语义为主。

### 6.2 帧级原子快照

这是创新点二的第一部分：

- FPGA在帧边界锁存完整特征。
- CPU按 `status/frame_id -> 特征组 -> status/frame_id` 双读规则取得一致快照。
- CPU用匹配的 `frame_id` ACK；陈旧ACK不能清除新快照。

### 6.3 配置原子提交

这是创新点二的第二部分：

- CPU先写 staging 区。
- 写 `CFG_COMMIT(config_seq)` 表示完整配置已准备好。
- 像素域只在VSYNC边界切换 active shadow。
- `CFG_STATUS`回报 active/pending序号，避免半新半旧阈值和ROI。

### 6.4 可解释OSD

显示内容至少包含轮次、识别颜色/形状/尺寸、目标判断、执行/不执行、理由、机械臂状态和错误码。CPU产生语义，FPGA渲染像素。

建议图表：APB窗口图、snapshot/ACK时序、staging/commit/active时序、OSD版式草图。

## 9. 第7章 实现、验证与资源分析（4-5页）

### 7.1 实现过程与问题闭环

按“问题-定位-方案-验证”选取2-3个真实案例，例如：

- Hard SoC硬件输入必须按XML/peri/SDC/IP/顶层/BSP原子批次处理。
- 双像素Even/Odd坐标对齐。
- Host能力与板级能力分层，避免接口未冻结时误触发动作。

### 7.2 分层验证矩阵

| 层级 | 证明什么 | 不能证明什么 |
|---|---|---|
| CPU Host | 分类、四任务规则、逐轮事务和负例 | 真实摄像头、MMIO、OSD和动作闭环 |
| RTL仿真 | 像素/寄存器/CDC模块行为 | 实际管脚和板级信号质量 |
| Efinity构建 | Map/PNR/STA/CDC/bitstream生成 | CPU取指、UART输出、APB实读和机械臂动作 |
| 板级测试 | 匹配批次的真实硬件现象 | 未覆盖接口或另一批次工程 |

### 7.3 FPGA资源与时序

当前可引用的G1固定批次数据：

| 项目 | 使用量 | 平台总量/说明 |
|---|---:|---|
| EFX_LUT4 | 11385 | Map报告 |
| EFX_FF | 10104 | Map报告 |
| EFX_RAM10 | 165 | Map报告 |
| EFX_DPRAM10 | 4 | Map报告 |
| Memory Blocks | 169 | 2688 |
| DSP Blocks | 0 | 1344 |
| Clocks | 12 | 96 |
| Setup WNS | +1.321 ns | PASS |
| Hold WHS | +0.026 ns | PASS |

必须注明：这是固定SHA单摄Hard SoC冷构建资源，不等于完整最终功能批次资源，也不等于板级闭环通过。

### 7.4 赛题指标对照表

使用列：`指标 | 官方要求 | 设计机制 | Host/构建证据 | 板级结果 | 状态`。

未测试的准确率、耗时和动作精度统一写“待同批次板级测试”，不得填理论100%或估算值冒充结果。

### 7.5 整场验收设计

说明20轮随机顺序、任务切换、目标/非目标、超时、放弃、故障和录像记录。完整方案内部目标可设置时间余量，但只有实际计时后才进入结果表。

## 10. 第8章 三项核心创新、总结与展望（2页）

### 8.1 三项核心创新

#### 创新一：无DDR、无回压的双像素旁路视觉特征提取

问题：CPU读取完整图像带宽高，旁路模块若参与握手可能干扰显示主链路。

方法：2PPC只读tap在流上累计ROI统计，帧边界锁存低带宽特征。

落点：`feature_stats_tap`、ROI/feature contract。

证据：RTL/构建/后续板级快照验证分层列出。

#### 创新二：双原子协议保障的FPGA-RISC-V芯机协同

问题：跨时钟域多位特征和多寄存器配置容易出现撕裂与半更新。

方法：`frame_id + valid/ACK`原子快照，以及`staging -> commit -> active`帧边界原子配置。

落点：APB寄存器契约、CDC规则和CPU board I/O。

价值：在保持FPGA并行处理能力的同时，使CPU能够安全、灵活地调整规则和参数。

#### 创新三：面向评分链的一轮一事务可解释安全闭环

问题：连续视频帧可能重复触发机械臂，且评委要求每轮输出识别、判断、动作及理由。

方法：逐轮锁存、唯一动作请求、SKIP理由、OSD语义、单请求机械臂事务和fail-closed故障收敛。

落点：round controller、transaction、result semantics、OSD和arm controller。

价值：将比赛评分链直接落实为可审计的系统行为。

### 8.2 总结与展望

总结只复述真实完成的设计和验证；展望明确列出尺寸标定、板级APB/OSD闭环和机械臂安全Gate等尚未完成项。

## 11. 参考文献与附录

### 参考文献

- 官方比赛细则和赛题材料。
- Efinix官方开发板、SoC、MIPI和工具文档。
- myCobot官方协议与产品资料。
- 第九届优秀作品中与FPGA视觉、分拣机器人和综合图像处理直接相关的案例。

### 附录建议

- 附录A：FPGA/CPU寄存器偏移与字段表。
- 附录B：四任务真值表、理由码和错误码。
- 附录C：关键参数与标定表。
- 附录D：测试矩阵、构建命令、SHA和制品哈希。
- 附录E：主要模块与队内工作分工（保持盲审，不写个人身份）。

## 12. 明天制作前的材料清单

- 系统架构图原稿。
- FPGA双像素旁路统计框图。
- APB原子快照和原子配置时序图。
- CPU逐轮事务状态机图。
- 机械臂动作状态机和安全Gate图。
- OSD版式或真实画面。
- Efinity资源、STA和CDC报告截图。
- Host测试日志摘要。
- 开发板、摄像头、机械臂和完整系统脱敏照片。
- 四任务测试/待测矩阵。

## 13. 实物摄影执行清单

### 13.1 通用拍摄规范

- 使用手机原生相机最高画质，优先4:3，不开人像虚化、滤镜、美颜或数码变焦。
- 拍摄前擦净镜头；关闭时间水印、手机型号水印和定位水印。
- 使用均匀白光，避免顶灯在开发板和显示器上形成强反光。
- 背景使用干净的浅灰、白色或深灰桌面；移除个人物品、学校标识、姓名贴和无关线缆。
- 手机尽量与被摄平面平行；有条件使用支架，避免广角畸变和手抖。
- 每个机位至少拍一张横图和一张竖图，并多留约15%的四周裁切空间。
- 不通过聊天软件压缩原图；保留原始照片和拍摄时间。

### 13.2 最小必拍8张

| 编号 | 拍摄对象与机位 | 建议距离/焦段 | 画面要求 | 文档用途 |
|---|---|---|---|---|
| P01 | 完整系统45度总览 | 1.2-1.8m，手机1x | 开发板、摄像头、显示器、机械臂和工作区同时入镜；线缆整理清楚 | 封面内页、系统总体方案 |
| P02 | 完整系统正俯视 | 1.0-1.5m，手机1x | 信号流方向清晰，四周留白；不要让拍摄者倒影入镜 | 系统组成与物理布局 |
| P03 | Ti375开发板俯拍 | 35-50cm，手机1x或2x光学 | 板卡完整、芯片和接口清晰；镜头与PCB尽量平行 | 硬件平台与接口说明 |
| P04 | 摄像头及其连接 | 25-40cm，手机2x光学优先 | 摄像头模组、排线接口和安装方向清晰；同时补一张侧面固定方式 | MIPI采集与机械结构 |
| P05 | myCobot整机安全姿态 | 1.0-1.5m，手机1x | 基座、夹爪、抓取区和放置区完整入镜；机械臂保持固定安全姿态 | 机械臂控制方案 |
| P06 | 夹爪与物块特写 | 30-50cm，手机2x光学 | 展示夹持接触面、物块尺度和安全高度；不要为了拍照临时触发动作 | 轻取轻放与末端执行器 |
| P07 | 五色三形状三尺寸物块 | 60-90cm，手机1x | 按颜色或形状整齐排列，加入直尺但不遮挡物块 | 识别对象与测试集 |
| P08 | 摄像头到HDMI运行画面 | 屏幕正前方0.8-1.2m，手机1x | 屏幕边框和系统硬件局部同时入镜，证明是实时链路；另拍一张只含屏幕内容的近照 | 当前板级已运行能力 |

### 13.3 有条件补拍6张

| 编号 | 拍摄内容 | 要求 | 可支持的结论 |
|---|---|---|---|
| P09 | 拨码目标输入特写 | 清晰拍到拨码编号与当前组合；旁边放一张不含姓名的任务配置卡 | 现场目标配置方式 |
| P10 | HDMI OSD结果画面 | 等CPU到OSD打通后，拍到颜色、形状、尺寸、目标判断、动作/理由 | 可解释结果输出 |
| P11 | 机械臂抓取阶段 | 只在既有安全流程和人工操作者控制下拍摄；连拍接近、夹取、抬升 | 机械臂独立工作现象 |
| P12 | 机械臂放置终态 | 拍到起点、旋转后放置区和最大臂展关系；可用地面标记线辅助说明 | 180度放置设计或实测证据 |
| P13 | 调试接口局部 | 只拍经过确认的接口和线缆标签，不用照片推断未验证电平/管脚 | 接口实现过程 |
| P14 | 团队操作场景 | 若封面或附录需要，可从背后/侧后拍摄，不露脸、不出现学校标识 | 工程实施过程，不作为技术证据 |

### 13.4 视频与动作截图原则

- 不为拍照新增机械臂动作；只记录团队已经安全验证并由现场人员控制的流程。
- 动作图最好从固定机位视频中截取三个关键帧：夹稳并抬离台面、搬运途中、轻放后夹爪退出。
- 图片标题必须区分“机械臂独立运行”“系统闭环运行”。CPU-FPGA尚未连通时，不能把独立动作照片标成自主识别抓取。
- 若来不及获得CPU到OSD画面，正文使用OSD版式设计图并标记“设计示意”，不得伪装成实拍。

### 13.5 素材命名建议

使用 `日期_编号_内容_状态` 命名，例如：

- `20260720_P01_系统总览_实拍.jpg`
- `20260720_P08_摄像头到HDMI_已运行.jpg`
- `20260720_P10_CPU结果OSD_待验证.jpg`
- `20260720_P12_机械臂放置终态_独立运行.jpg`

## 14. 待补信息与截止前降级策略

| 优先级 | 所需信息/素材 | 建议提供者 | 截止前取得时 | 截止前仍缺失时 |
|---|---|---|---|---|
| P0 | 队员姓名及封面署名顺序 | 团队负责人 | 写入封面，不进入正文 | 封面仅写队伍编号CICC1002207 |
| P0 | 摄像头到HDMI原始照片 | FPGA队友/现场摄影 | 作为板级已运行现象 | 仅引用已有调试记录，不放无来源照片 |
| P0 | 当前最终Git SHA和文档冻结时间 | 集成人员 | 固定文档证据基线 | 在文档中注明“截至提交前最后确认批次”并保留SHA占位 |
| P0 | 最新Efinity资源、STA、CDC报告 | FPGA队友 | 替换G1固定批次表，并绑定同批SHA | 保留G1表，明确其不是完整最终功能批次 |
| P0 | CPU-FPGA APB/OSD板级结果 | CPU+FPGA队友 | 加入MAGIC、快照、结果回写和OSD实拍 | 作为完整方案设计写作，结果栏标“待板级验证” |
| P1 | CPU Host最新测试命令与计数 | CPU负责人 | 更新验证矩阵 | 不写旧计数，只写测试范围和状态 |
| P1 | 机械臂独立工作视频/照片及动作口径 | 机械臂负责人 | 写为“机械臂独立验证”，若闭环另有证据再升级 | 只写控制方案，不放成功率和耗时 |
| P1 | 颜色/形状/尺寸标定数据 | 视觉/FPGA队友 | 给出混淆矩阵、阈值或bbox标定图 | 只写算法与标定方法，准确率填待测试 |
| P2 | 20轮整场计时和成功记录 | 全队 | 写入最终性能表 | 保留验收设计，不声明整场通过 |
| P2 | 功耗或温升数据 | FPGA/现场队友 | 说明仪器、条件和实测结果 | 整章不写功耗数字，不使用“低功耗”定量结论 |

### 你现在最适合补充的内容

1. 封面是否最终写队员姓名；若写，提供完整姓名和署名顺序。
2. 当前CPU侧你亲自运行过的测试命令、日期、终端输出或日志路径。
3. 你对拨码位定义的已确认版本；未冻结则只提供现状，不猜测最终字段。
4. 机械臂“可以工作”的准确边界：PC控制还是板控、完成过哪些动作、是否涉及180度放置。
5. 拍完P01-P08后提供原图目录，后续由文档制作阶段统一挑选、裁切和加图题。
