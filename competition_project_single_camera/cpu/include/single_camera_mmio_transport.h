#ifndef SINGLE_CAMERA_MMIO_TRANSPORT_H
#define SINGLE_CAMERA_MMIO_TRANSPORT_H

#include "single_camera_runtime.h"

/* Placeholder only: every operation is unavailable until an audited ABI exists. */
void sc_mmio_transport_init_fail_closed(sc_runtime_transport_t *transport);

#endif /* SINGLE_CAMERA_MMIO_TRANSPORT_H */
