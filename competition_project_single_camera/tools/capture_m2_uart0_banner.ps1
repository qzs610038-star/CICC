[CmdletBinding()]
param()

$ErrorActionPreference = 'Stop'

throw 'HISTORICAL_SUPERSEDED: this UART0/R0 capture tool must not select a COM port, open a serial device, or be used for the I0 UART1 route. Use the fixed-SHA H0-H6 receipt packet and an approved UART1 tool supplied with the libaoxun fixed commit.'
